# PROJECT_STATE.md

Point-in-time snapshot of the actual repo state. Update this file when the state changes materially — do not let it drift into aspirational territory.

**Last updated:** 2026-07-17 (Underwater Phase 1 "Living Water" v0.1 now committed as `e1c576b`, see `AUDIT.md` Entry 44; Underwater Phase 2 "Bloom refinement" v0.1 — plankton flow-field alignment + pulse-train brightness waves — plus a priority perf-fix addendum (forced Bloom Event High: 49.9-50.1fps → 65.1-65.4fps) now committed as `faddaba`, see `AUDIT.md` Entry 45 and its addendum; Abyssal Bloom Phase 3 "Distant Event" v0.1 — a new distant abyssal glow field, gated across the same bloom arc — now committed as `fed7a7d`, see `AUDIT.md` Entry 46; Abyssal Bloom Phase 4 "Presence / Color / Depth Population" v0.1 — a distant alien-presence shadow layer, color-variation nudges, and 4-8 cheap background jellyfish, plus a compositing-order visibility fix (Addendum 1) — committed as `28f3e43`, see `AUDIT.md` Entry 47 and its addendum; **Cosmic Reef Pivot Phase 1 v0.1 — World04 renamed "Cosmic Reef", pivoting from a purely underwater scene toward a psychedelic underwater-to-cosmic transformation (starfield, nebula retint, color-bloom wave, ribbon organisms, hero-jellyfish demotion, `uCosmic` accumulator tied to the same evolution-length slider as bloom) — committed as `1b51baf` at the user's explicit request, still NOT ready for review: the mandatory 60fps-floor performance check FAILED at sustained high bloom (~50fps measured, vs. the required ≥60fps) after two genuine optimization rounds; see `AUDIT.md` Entry 48 for the full investigation and its options memo.**)

**Video Atoms Phase 3 "Effect Stack v1" (World05 HybridTest):** uniform-driven grayscale/mirror-X/
mirror-Y/lift-gamma-gain grade/vignette added to the existing `hybrid.frag` composite shader after the
Phase 1 `mix(video, child, uBlend)`, plus decode-rate playback-speed pacing (0.25x-2x) in
`FfmpegPipeDecoder`'s reader thread, and matching `Tuning.cs`/`ControlServer.cs` dashboard sliders under
a new "Effects" section — see `AUDIT.md` Entry 50. Not committed as of this note without the paired
commit hash; check `git log` for the actual commit once landed.

**Media Console Audio Tuning slider bug fixed (v0.1):** the Audio Tuning tab's per-mapping sliders
(Liquid Warp/Edge Glow/Mirror/Color-Saturation) could appear to stop responding entirely if more than
one browser tab/page-load held the panel open — a full-object last-write-wins save race, root-caused
and fixed via a new per-field patch endpoint (`POST /api/audio/tuning/field`). See `AUDIT.md` Entry 52.
Verified via synthetic test-pulse signal only (user was away); real two-guitar tuning session with the
fix in place is still pending.

**Entry 38 blocker status: appears resolved on this machine.** This session's own smoke-test/build logs show
`[AudioEngine] Capture opened: device="Clarett 4Pre USB", ...` (not "Hue Sync Audio") — the user has since
selected the real interface as the default input, matching Entry 38's own recommended fix. Not re-verified
against a live guitar signal this pass (out of Phase 3's turbine-geometry scope), but the device-selection
symptom itself is gone. Entry 34's Refinement Pass 2 and Entry 38's investigation were reviewed and
committed together as `1615b12` (`CLAUDE.md`'s unrelated Entry 29 dirty content was excluded from that
commit, same precedent as the Entry 31 pre-commit cleanup) — see the Wind Turbine Fire Phase 3 bullet below
for the next pass built on top of that commit, since committed as `81e58fa`.

**Historical blocker record (Entry 38, this machine — not a code bug):** with the Entry 37 AMFI
workaround finally letting the app run long enough to observe live, the user reported Wind Turbine Fire
showing dark spinning turbines with no fire and no audio reaction at all. Live reproduction (dashboard-only
+ `POST /calibration/testinput` test pulses, plus the project's established temporary-forced-heat
`--diagnostic motion` capture technique, fully reverted) found **zero code defect** — the shader/scene
render fire correctly when actually driven (see `DiagnosticReports/Motion_20260713_093550/`), and the
`CalibrationEngine` test-pulse pipeline is fully correct end-to-end. Root cause: this Mac's current macOS
default audio input device is **"Hue Sync Audio"** (a Philips Hue Sync virtual device), not a real guitar
interface — confirmed via the app's own `[AudioEngine] Capture opened: device="Hue Sync Audio"` startup
log and `system_profiler SPAudioDataType` (which also shows no Focusrite Clarett present in the device list
at all on this machine). `Audio/AudioEngine.cs` opens the OS default capture device by design
(`ALC.CaptureOpenDevice(null, ...)`, no device-selection mechanism exists in this codebase), so real guitar
audio never reaches the app regardless of how loud the user plays — `G1/G2 Level/Bass` stay at exactly
`0.000`. **User action needed:** select the real interface as the default input in System Settings → Sound
→ Input (confirm it's connected/recognized first if it doesn't appear in that list), then relaunch. See
`AUDIT.md` Entry 38 for full investigation detail and evidence.

**Prior, separately-resolved blocker (Entry 37) — RESOLVED via workaround, this machine:** Entries 35/36 root-caused the
Desktop shortcut / `run-show.sh` / `dotnet run` failing with zero output and a near-instant SIGKILL to
macOS AMFI/Gatekeeper rejecting the locally-built, ad-hoc-signed .NET apphost binary
(`bin/Debug/net8.0/CosmicEngine.App`) at `exec()` time, before any Cosmic Engine code runs — confirmed via
`log show`: `AMFI: ... is adhoc signed` / `amfid: not valid: Error Domain=AppleMobileFileIntegrityError
Code=-423`. Rather than requiring a reboot or a machine-wide Gatekeeper/Developer-Mode change, Entry 37
found and verified a **development-workflow fix that avoids the rejected binary entirely**: `CosmicEngine.
App.csproj` now sets `<UseAppHost>false</UseAppHost>` (no ad-hoc-signed apphost is generated at all, and
`dotnet run` itself now works as a side effect — it execs via the trusted `dotnet` host instead), and
`run-show.sh`/`Run Cosmic Engine.command` now build once then launch the built `.dll` directly via `dotnet
bin/Debug/net8.0/CosmicEngine.App.dll --dashboard-only` rather than `dotnet run`. Verified end-to-end:
build, all four required smoke tests (StellarNursery Safe seed 777, LavaLamp Safe, WindTurbineFire Safe,
plus a High-profile run), and a full launcher cycle (dashboard-only → scene launch from dashboard → quit)
all pass cleanly with zero orphan process after every run, on both the external-drive and internal-disk
copies. **Not a code regression** — `AudioEngine.cs` and all recent Wind Turbine Fire changes remain
unimplicated. No machine-wide security setting was changed: Gatekeeper was not disabled, PACE Eden/
`licenseDaemon` was not touched, Startup Security Utility/Recovery Mode was not touched. See `AUDIT.md`
Entry 37 for full investigation and verification detail, and Entries 35/36 for the original root-cause
investigation.

**Project folder note:** the app project folder is now named `CosmicEngineApp` (no dot) — it was `CosmicEngine.App` through Entry 22, renamed in Entry 23 because macOS Finder mistakes any folder ending in `.app`/`.App` (case-insensitive) for a broken application bundle and blocks normal navigation into it. The `.csproj` filename (`CosmicEngine.App.csproj`), the C# namespace (`CosmicEngine.App.*`), and the `.sln`'s internal project display name are all unchanged — only the directory name changed. Historical entries below that reference `CosmicEngine.App/...` paths describe the repo as it was at the time and were not retroactively rewritten.

## Branch / status

- **Branch:** `cosmicos`
- **Phase 1 Hybrid Proof (video-atoms MILESTONE_BREAKDOWN.md): implemented, NOT ready for review** —
  committed (`380614d`/`454392e`/`58ffac6`), not pushed. `IVideoDecoder`/`FfmpegPipeDecoder`/
  `VideoTexture` + new `HybridTestScene` (World05) compositing a hardcoded VisionBoard clip with a
  StellarNursery child world. **Critical gap: no ffmpeg installed on the dev machine, so no real
  video was ever decoded/displayed this pass** — see `AUDIT.md`'s entry and
  `DiagnosticReports/Phase1HybridProof_20260717_133653/PHASE1_SUMMARY.md` for the full honest
  breakdown before treating this as a completed milestone.
- **Star Artifact Fix (Entry 7): reviewed and ACCEPTED by ChatGPT** on 2026-07-05, committed as `1051e27`. Remains intact and unmodified — see below.
- **Visual Recovery Pass 1 (Entry 8): reviewed and ACCEPTED by ChatGPT** on 2026-07-05, committed as `3cf74ea`. Accepted as a visual recovery baseline, not final art.
- **Visual Detail Pass 1 (Entry 9): reviewed and REJECTED by ChatGPT** on 2026-07-05 — hard-edged/posterized/masked artifacts from a `step()` density gate.
- **Visual Detail Pass 1 Revision (Entry 10): reviewed and ACCEPTED by ChatGPT** on 2026-07-05, committed as `1150667`. Reviewer flagged the accepted result as still soft/blurry/low-frequency/under-detailed, requiring a follow-up detail pass.
- **Visual Detail Pass 2 (Entry 11): REJECTED / PARKED** on 2026-07-06 (not committed — the `stellar_nursery.frag` diff remains uncommitted in the working tree, undisturbed). Further pure-GLSL density-detail tweaking is paused pending the Fable Strategy Review pivot.
- **Fable Strategy Review — Roadmap Pivot (Entry 12):** documentation-only pass, not yet committed. Decision: Stellar Nursery visual polish is parked (not abandoned) until infrastructure (`RenderScale`/Live-Safe profiles) and hardware/audio validation (OptiPlex, real guitar) are done. See `ROADMAP.md` for the new P1-P6 order.
- **P1 — RenderScale and Live/Safe Profiles (Entry 13):** implemented, reviewed and **ACCEPTED by ChatGPT** on 2026-07-06, committed as `301a2ab` and pushed. `RenderScale` now actually resizes the `RenderTarget` (`Safe` 0.50 → 640x360, `Balanced` 0.75 → 960x540, `High` 1.00 → 1280x720, default `High`); `--profile <name>` CLI flag with warning+fallback-to-Safe on an invalid name; `[Perf]` logs profile name, RenderScale, and actual render target size; `--diagnostic perf-sweep` runs 10 bounded sub-runs per profile in one process. 10-run evidence: `Safe` rock-solid (58.87-59.75 fps, stdev 0.26), `High` bimodal (18.94-59.76 fps, stdev 16.43 — 3 of 10 runs collapsed to ~19-47 fps) — a real, reproducible FPS-collapse pattern, root cause still open (see below).
- **Lava Lamp Scene Draft v0.1 (Entry 14): reviewed and ACCEPTED by ChatGPT** on 2026-07-06, committed as `c29a219`. Implemented a second world, `Worlds/World02_LavaLamp/LavaLampScene.cs` — a cheap analytic 2D metaball-field fullscreen shader, proving the `IWorld` architecture supports multiple scenes. New `Engine/WorldSelector.cs` adds `--world <name>` CLI selection (mirrors `--profile`'s case-insensitive/warn-and-fallback UX), wired into `CosmicEngine.cs` and `PerformanceSweep.cs`; default world unchanged (`StellarNursery`). No Stellar Nursery shader/code changes — `stellar_nursery.frag`/`StellarNursery.cs` diff is zero. Lava Lamp's own 20-run perf-sweep (10 × Safe, 10 × High) shows near-zero FPS variance (range 0.2-0.4 fps) — markedly more stable than Stellar Nursery's documented `High`-profile bimodal collapse, satisfying the performance guardrail without shader simplification. Accepted as a v0.1 prototype draft, not final art — see `AUDIT.md` Entry 14 for reviewer limitations noted.
- **Scene Dashboard v0.1 (Entry 15): reviewed and ACCEPTED by ChatGPT** on 2026-07-06, committed as `1f4dc01`. Added a local dashboard (served by the existing `ControlServer` at `http://localhost:8080`) for launching/switching scenes without CLI flags. New `Engine/SceneRegistry.cs` is now the single source of truth for scene metadata (Id/DisplayName/Description/Status/DefaultProfile/Showable/Factory) — `Engine/WorldSelector.cs` was refactored into a thin wrapper over it so CLI `--world` parsing and the dashboard's scene cards share one registry. `Engine/CosmicEngine.cs` gained thread-safe `RequestSwitch`/`RequestRestart`/`RequestQuit` methods, applied once per frame on the render thread, enabling **live, in-process scene AND profile switching** (verified: same PID throughout a full `StellarNursery → LavaLamp Safe → LavaLamp High → Restart → StellarNursery → Quit` cycle, driven live through a browser against a real `dotnet run -- --profile Safe` session) — this is the task's preferred behavior, not the CLI-hint fallback. New `run-show.sh` / `Run Cosmic Engine.command` launchers start the engine in unbounded "show mode" and auto-open the dashboard. Dashboard-driven requests are explicitly gated off during any bounded `--smoke-test`/`--diagnostic` run, and all four existing CLI commands were re-verified unchanged. No Stellar Nursery/Lava Lamp shader or visual changes — zero diff on any scene file.
- **Stellar Nursery Showability Audit (Entry 16), Revision (Entry 17), and Consistency Fix (Entry 18): reviewed and ACCEPTED by ChatGPT** on 2026-07-07, all three committed together as `2a713e7` (never committed individually since review moved directly from one to the next). Not yet pushed.
- **Stellar Nursery Showability Audit (Entry 16):** user reported Stellar Nursery, launched via the dashboard, looked like a static purple/pink gradient with no stars/structure/motion. Confirmed accurate and root-caused to three compounding issues, all fixed with minimal, targeted parameter changes (no shader rewrite): (1) density field time-evolution was mathematically updating but ~25x too slow to be perceptible within 15s — fixed by raising the `fbm3D()` time coefficient; (2) point stars were present and correctly shaped but too sparse (only 1-2 visible per frame) — density roughly doubled; (3) **the dominant cause** — an unconstrained random per-launch seed had roughly a 60-70% chance (16 seeds sampled) of landing far from `nebulaDensity()`'s threshold, producing a flat, structureless gradient (exactly the user's report, in various hues) — mitigated by restricting `StellarNursery.Load()`'s random seed to a small pool (400, 33, 610, 5) empirically confirmed to show real cosmic structure. **Outcome: RESTORED**, not marked not-showable — verified via a new `--diagnostic motion` mode (engine instrumentation, captures the real render path at t=1s/5s/15s) showing clear, human-visible structural morphing on every launch, with no regression vs. the last accepted baseline (Entry 10) and zero Lava Lamp changes. See `AUDIT.md` Entry 16 for full detail, including the honest limitation that the curated-seed pool is a mitigation, not an architectural fix.
- **Stellar Nursery Showability Revision (Entry 17):** user + ChatGPT review of Entry 16's fix found structure sitting mostly at frame corners/edges (empty dead-center) and stars barely visible even after Entry 16's increase. Root-caused to `compositionOffset` being far too small (`-55,30,0`, tens of ly) relative to the dominant density octave's ~1000 ly scale to meaningfully relocate sampled structure — changed to `(0,-400,0)`. This invalidated the Entry 16 seed pool, requiring full re-curation at full resolution (a thumbnail-only pass was actively misleading) and, newly, at **both t=1s and t=15s** — discovered that 2 of 4 initial candidates (480, 88) looked good at launch but flattened toward empty by t=15s, since Entry 16's faster time-evolution means the same seed-dependent flatness fragility can now occur *during* playback, not just at launch. Final pool: `{777, 33, 61, 155}`, all verified robust at both timepoints. Star color multiplier raised `0.35→1.6` and radius increased `0.07/0.055→0.13/0.10`. The *visual fix itself* was sound, but **Entry 17's package was REJECTED by ChatGPT** — see Entry 18: the t1/t5/t15 "proof" screenshots were captured with an uncontrolled random seed (not necessarily 777, the seed used for the separately-captured reference frame), so the package's own evidence didn't actually demonstrate what it claimed.
- **Stellar Nursery Showability Consistency Fix (Entry 18):** root-caused Entry 17's rejection to a capture-methodology bug, not a rendering bug — `after_showability_revision_full.png` used `COSMICENGINE_SEED=777` explicitly, while `after_showability_revision_t1/t5/t15.png` were captured via `--diagnostic motion` with no seed control, so `StellarNursery.Load()` picked a random pool member (confirmed, by visual comparison, to be a softer one, not 777) — and neither diagnostic report logged which seed was used, so the mismatch went unverified before the (false) "same frame" claim was made. Fixed by adding a new `--seed <value>` CLI flag (sets `COSMICENGINE_SEED` for the process, works for normal/dashboard/all diagnostic modes) and logging the active seed in every `--diagnostic motion`/`--diagnostic visual` `REPORT.md` going forward (`StellarNursery.Seed` new public property + `ActiveWorldSeedInfo()` helper). Recaptured all acceptance screenshots with explicit, matched `--seed 777` throughout — new t1/t5/t15 frames are confirmed consistent with the (correctly-captured) reference frame via matching metrics and matching star position/brightness. **Outcome: StellarNursery showable = yes, when launched with a controlled/known seed.** No shader/color/star/composition changes in this pass — capture-methodology fix only. Zero Lava Lamp changes. See `AUDIT.md` Entry 18, including the honest limitation that softer pool members (61, 155) can still appear on an uncontrolled dashboard launch.
- **Dashboard Show Seed Support (Entry 19, this pass, not yet committed):** small follow-up addressing Entry 18's accepted limitation directly — the dashboard could still launch Stellar Nursery via random pool selection. New `SceneDefinition.ShowSeed` (`"777"` for Stellar Nursery, `null` for Lava Lamp) is now applied automatically: at initial startup (unless an explicit CLI `--seed` was given) and unconditionally on every dashboard-driven `POST /launch` switch into a scene. The dashboard now displays the active seed in its status bar (`Seed: 777.00`) and each scene card (`Show Seed: 777`), via new `GET /status`/`GET /scenes` fields. CLI `--seed` and plain CLI random-pool selection are both unchanged/still work — confirmed via a `--seed 33` test showing the override correctly wins. Verified live via a real dashboard browser session (status bar and card both showed seed 777, console log confirms `[Seed] Using show seed 777 for Stellar Nursery.`) and via bounded CLI smoke-tests. Zero Lava Lamp changes, zero Stellar Nursery shader changes. See `AUDIT.md` Entry 19.
- **Original bug fixed (Entry 7):** the square/rectangular star artifacts are fixed — stars now render as small bounded radial points. **Unaffected by Entries 8-19** — re-confirmed via close-up crop in each pass's package, including Entry 14, 16, 17, and 18 (raw pixel grid shows a clean, smooth radial falloff, no cell-square). Entries 15 and 19 did not touch either scene's shader/code at all.
- **Mac mini Baseline Validation (Entry 20):** hardware-baseline pass on the user's new Mac mini M4 Pro (Mac16,11, 12-core, 24GB), which is now the active development/art-review machine — **the old 2015 MacBook Pro / Intel Iris 6100 is retired from that role and is historical context only.** `dotnet build` succeeds (0 warnings/errors). OpenGL: renderer `Apple M4 Pro`, vendor `Apple`, version `4.1 Metal - 90.5`, GLSL `4.10`. All four world/profile smoke tests clean (~74-75 fps each). 10-run `--diagnostic perf-sweep` executed for both worlds (StellarNursery via default, LavaLamp via `--world LavaLamp` — the sweep only covers one world per invocation, not both at once): StellarNursery Safe/High and LavaLamp Safe both stable (range ≤1.5 fps); **LavaLamp High showed a real 12.4 fps range (63.9-76.4) across 10 runs** — a new, smaller-magnitude echo of the FPS-dip pattern previously seen on StellarNursery High on the old Iris hardware (Entry 13), now on a different world/hardware combination, root cause not yet investigated. Dashboard launcher (`run-show.sh`) verified end-to-end: starts, `http://localhost:8080` live, in-process scene+profile switch confirmed (`StellarNursery/Safe → LavaLamp/High`, same PID), quit works, no orphan process after any command in this pass. **A real bug was found and fixed mid-pass, with explicit user approval:** `--diagnostic perf-sweep` was leaving 5+ Cosmic Engine windows open simultaneously on screen (user directly observed and flagged this) — root-caused to `RunSweepSubRun()` in `Engine/CosmicEngine.cs` never calling `_window.Dispose()` between its 20 per-sweep sub-runs (`GameWindow.Close()` only stops the render loop, not the native window), fixed with a single `finally { _window.Dispose(); }`. User visually confirmed the fix live. See `AUDIT.md` Entry 20 for full detail, including the `dashboard_home.png` screenshot-capture limitation. Both Entry 19 and Entry 20 were subsequently committed as `33be284`/`fd48c59` and pushed.
- **Stellar Nursery Art Restoration Pass 1 (Entry 21): reviewed by ChatGPT as ACCEPTED WITH PRE-COMMIT CORRECTIONS** on 2026-07-10, corrections applied and committed as `3cb1608` (not yet pushed). Direct response to user feedback that the Mac mini baseline Stellar Nursery had regressed into mostly purple wisps (sparse stars, little heat, no starbirth, no bodies/mass, no orange accents, thin dust). Added, all additive to `stellar_nursery.frag`: `massField()` (low-frequency noise biasing WHERE density survives threshold, creating several irregular organic "body" masses instead of one uniform cloud), a `coreGlow` warm orange-red emission term tied to those bodies and textured with existing fine noise, `starbirthCore()` (small bright bounded warm-white points inside dense bodies only, same shape-safety principle as `pointStarLayer` — zero diff on the actual star code), and a second crossing dust-lane erosion pass. Metrics at seed 777: avg luminance +60% (0.132→0.212), warm-pixel estimate ~44x (0.4%→17.9%), center-region luminance +54%. 12/14 visual-checklist items PASS; two honest partial results, both explicitly accepted-with-limitations by the reviewer: starbirth accents present but subtle, and the dominant warm mass region still reads somewhat smooth/rounded at its core despite iteration (9 rounds, including catching and fixing a real "flat peach blob" saturation failure in iteration 2 before it was ever reported as a result). All preserve requirements verified intact via zero diff (seed pool, star code, `ControlServer.cs`, `SceneRegistry.cs`) or direct re-test (`--diagnostic motion` confirms unchanged motion characteristics). One unrelated fix, confirmed intentional/user-approved and kept in the same commit: `StartFocused = false` added to `Engine/CosmicEngine.cs`'s `NativeWindowSettings`, since bounded test-run windows were stealing OS focus from other apps. Surfaced (but did not fix, out of scope) an environmental FPS anomaly — absolute numbers far above the ~74-75 fps Entry 20 baseline, reproducing on LavaLamp too (zero shader diff), most likely macOS suspending VSync pacing for a non-frontmost window. **These elevated numbers are not the new performance baseline** — the ~74-75 fps Entry 20 Mac mini figure remains the reference until this anomaly is separately investigated. Accepted as an art recovery baseline, not final Stellar Nursery art — see `AUDIT.md` Entry 21 for full detail, including the reviewer sign-off and required next phase (Pass 2, targeting only the smooth-mass-core and starbirth-subtlety gaps).
- **Desktop Launcher Usability Pass (Entry 22): reviewed by ChatGPT as ACCEPTED WITH PRE-COMMIT CORRECTIONS** on 2026-07-11, corrections applied. The user asked for a Desktop-double-clickable launcher requiring no Terminal use. Rewrote `run-show.sh` and `Run Cosmic Engine.command` (originally from Entry 15): robust symlink/Finder-alias-following path resolution (never assumes CWD), dependency checks (`dotnet`/csproj/`curl`) with clear kept-open error messages instead of a vanishing window, duplicate-running-instance detection (opens the existing dashboard rather than starting a second engine), poll-based dashboard-ready detection replacing a fixed sleep, engine output redirected to a log file with the last 40 lines shown directly in the window on failure, and (pre-commit correction) an explicit cleanup trap so an interrupted/terminated launcher stops only the engine process it started itself — verified directly with a real interrupt test (job-control-enabled shell, matching actual Terminal.app behavior), confirming zero orphan afterward, and confirming normal dashboard-Quit still exits silently with no scary error output. New `README_LAUNCHER.md` covers double-click instructions, step-by-step Finder-alias creation, Gatekeeper workarounds, and how to quit. Zero diff on any scene/shader/engine-source file — usability/launcher pass only. Known limitation: automatic Desktop-alias creation via `osascript`/Finder hung on an AppleEvent timeout and was abandoned in favor of the (verified-accurate) manual instructions, per the brief's own anticipated fallback. See `AUDIT.md` Entry 22 for full detail, including the reviewer sign-off.
- **Rename CosmicEngine.App → CosmicEngineApp (Entry 23):** committed as `db45b65`/`507893f` and pushed. Direct follow-up to Entry 22 — the user tried to reach `Run Cosmic Engine.command` via Finder and hit a broken/prohibited-icon folder that wouldn't open, defeating the whole "no Terminal" point of the launcher. Root cause: macOS Finder treats any folder ending in `.app`/`.App` (case-insensitive) as an application bundle. Renamed the directory via `git mv` (preserves file history), updated `CosmicEngine.sln`'s project path, and updated user-facing text in `run-show.sh`, `Run Cosmic Engine.command`, `README_LAUNCHER.md`, and `CLAUDE.md`. Deliberately scoped to the directory name only — `.csproj` filename, C# namespace, and `.sln` display name all still say "CosmicEngine.App" (minimal-risk choice, not a full rename). Verified: `dotnet build` succeeds both directly in the renamed folder and via `dotnet build CosmicEngine.sln` from the repo root; a Safe-profile smoke test confirms relative shader-loading paths still resolve correctly (75.1 avg fps, clean exit); `run-show.sh` re-verified end-to-end (dashboard up, seed 777, clean quit, zero orphan). See `AUDIT.md` Entry 23 for full detail, including the known limitation that a literal Finder screenshot re-confirmation wasn't possible this session (screen-recording access declined) — the fix is verified functionally and by root-cause elimination instead.
- **Dashboard-Only Launcher Mode (Entry 24):** committed. User feedback — the launcher previously opened the dashboard AND immediately started Stellar Nursery (default world + show seed 777), which the user does not want; double-clicking should open only the dashboard, with scene selection left to the user. Added `--dashboard-only` (new `Engine/DashboardHost.cs`, dispatched from `Program.cs`): starts `AudioEngine`/`ControlServer` and waits on the main thread (required for GameWindow/GL creation on macOS) for a scene to be picked via the dashboard, rather than constructing a `GameWindow` immediately. `ControlServer`'s `/launch`/`/quit` now route to `DashboardHost` when no `CosmicEngineApp` exists yet, falling through to the existing in-process switch/quit path unchanged once a scene is running. New `CosmicEngineApp.RunFromDashboardHost()` reuses every existing mechanism (seed application, `OnUnload` cleanup) unchanged — StellarNursery still gets seed 777 automatically. `run-show.sh` now launches `--dashboard-only` instead of `--profile Safe`; `README_LAUNCHER.md` updated to match. Verified: dashboard shows "No visual running. Choose a scene below." with no visual window opening automatically; Stellar Nursery (seed 777 confirmed) and Lava Lamp both launch correctly as the *first* scene from a fresh dashboard-only start (not just via in-process switch); dashboard Quit works in all three scenarios (mid-scene, pre-launch, full launcher cycle) with zero orphan process each time; direct CLI (`--world ... --profile ... --smoke-test`) unaffected. Zero diff on any scene/shader file. See `AUDIT.md` Entry 24 for full detail.
- **Lava Lamp v0.2 — Analog Liquid Light Prototype (Entry 25): reviewed by ChatGPT as ACCEPTED WITH PRE-COMMIT CORRECTIONS** on 2026-07-11, corrections applied and committed as `e40db2e` (not yet pushed). Targeted scene-improvement pass on `Worlds/World02_LavaLamp/Shaders/lava_lamp.frag` only (zero diff on `LavaLampScene.cs`, Stellar Nursery, or dashboard/launcher files), upgrading Lava Lamp from v0.1's "basic blob demo" toward an actual analog liquid-light/lava-lamp feel. Replaced the fixed circular orbit with rise/fall + two-frequency drift motion; added an angular "lobe" shape modulation breaking circular symmetry; added a second, slower/dimmer "back" layer for depth/parallax; added a 3-term sine-sum internal texture for soft marbling instead of flat gradient fill; re-tuned the IQ cosine palette warm-dominant (amber/orange/magenta front layer, cooler back layer, deep warm-red/purple background); reused the existing `uWobbleFreqMul` uniform for a subtle audio-reactive edge ripple — no new uniforms, no new audio infrastructure. Two real defects caught and fixed via a temporary, fully-reverted mock-audio-peak probe before being reported as results: a hue-flip bug (`uHueShift * 0.6` fully flipped the palette to blue/violet at peak audio, cut to `0.15`) and a grid-artifact bug (`innerTexture(wp * 6.0, ...)` produced an obvious dot/grid pattern, reduced to `wp * 1.4`). Metrics at Safe/High: warm-pixel estimate 1.1%/2.2%→52.3%/56.0% (~25-47x), cool-accent-pixel estimate 75.7%/75.3%→0.0%/0.0% — strong quantitative confirmation of the intended palette flip. Motion evidence (`--diagnostic motion`) confirms genuine merging/separating blob behavior across t1/t5/t15, not static or frantic. Performance unaffected: LavaLamp Safe/High smoke tests 75.0/75.1 avg fps, StellarNursery Safe smoke test 75.1 avg fps (confirms unaffected), 20-run perf-sweep shows tight single-mode variance (Safe range 1.8 fps, High range 0.3 fps) with no bimodal collapse. Dashboard-only mode re-verified end-to-end (idle → launch LavaLamp Safe → quit → zero orphan). Accepted as a prototype upgrade, not final art — known limitations: cyan/blue contrast is compositionally present but didn't register strongly in the simple per-pixel warm/cool metric; no real audio signal available in this environment (mock-peak probe only, not live guitar); `dashboard_after_lavalamp_launch.png` screenshot not captured (Chrome MCP unavailable, real-desktop screenshot attempt surfaced the user's unrelated personal browser activity and was declined) — substituted with a functional `/status`-based verification instead. See `AUDIT.md` Entry 25 for full detail, including the reviewer sign-off.
- **Dashboard Calibration Tab v0.1 (Entry 26): reviewed by ChatGPT as ACCEPTED WITH PRE-COMMIT CORRECTIONS** on 2026-07-11, corrections applied and committed as `095d013` (not yet pushed at the time; both `095d013` and its `318d1e9` CLAUDE.md follow-up have since been pushed). User-tested directly on the Mac mini with a real Clarett interface and guitar plugged into Input 1: "The meters and the response curves work as expected. This is a great outcome" — the Sensitive preset was confirmed lifting the same raw playing dynamics into a meaningfully higher, more usable output range than Linear. New "Calibration" dashboard tab addressing the user's reported "too on/off, not a smooth response curve" problem. New `Audio/InputCalibration.cs` (per-input gain/gate/smoothing/ceiling + a cubic-Bezier response curve modeled on eDRUMin's two-point curve editor, plus live peak/RMS/clip tracking) and `Audio/CalibrationEngine.cs` (selects which 2 of the currently-available audio channels drive "Input A"/"Input B", ticks both on a self-starting ~30Hz background timer). Six new `ControlServer.cs` endpoints (`GET /calibration/status`, `POST /calibration/channels|manual|preset|curve|reset`). Dashboard UI: a small tab-bar (`Scenes / Show` | `Calibration`) wrapping the existing page content unchanged, plus the new Calibration tab with an audio-status line, two channel selectors (default Input 1/Input 2, matching the existing 2-channel stereo capture), two DAW-style level meters (raw/smoothed/peak/peak-hold/clip/target-zone/post-curve-output), and two per-input response-curve editors with draggable control points (not deferred to v0.2 — implemented directly), four presets each (Linear/Sensitive/Compressed/S-Curve), and manual gain/gate/smoothing/ceiling sliders with a reset button. Channel count is honestly reported as 2 (the real current stereo-capture limit) rather than faking 8-channel Clarett support — the model is channel-count-agnostic so real multi-channel capture can be wired in later without a UI rewrite. Scenes do **not** yet consume the calibrated post-curve values — Stellar Nursery and Lava Lamp are both zero-diff, still using their original raw `AudioSignal` path, a deliberate choice per this pass's explicit "do not change scene visuals" constraint. Verified: `dotnet build` 0 warnings/errors; StellarNursery Safe and LavaLamp Safe smoke tests both ~75 fps; full `--dashboard-only` cycle (idle → Calibration tab renders correctly → all 6 endpoints exercised successfully → StellarNursery launches with seed 777 → switches to LavaLamp → clean quit → zero orphan process). No `.png` screenshots could be produced in this environment (in-app browser preview has no save option, Claude-in-Chrome extension not connected, real-desktop capture was either unreachable from this session's filesystem tools or correctly declined as a privacy risk) — substituted with a full page-text/accessibility-tree capture, a complete calibration-endpoint transcript, and scene-launch `/status` evidence, honestly documented as a known limitation rather than faked. See `AUDIT.md` Entry 26 for full detail, including the reviewer sign-off.
- **Calibrated Audio Reactivity Integration v0.1 (Entry 27): reviewed by ChatGPT as ACCEPTED WITH PRE-COMMIT CORRECTIONS** on 2026-07-12, corrections applied and committed as `e163c16` (not yet pushed). Wires `CalibrationEngine`'s post-curve output (confirmed working by the user's own hardware test, Entry 26) into scene audio reactivity — the step that was explicitly left undone in Entry 26. Investigation confirmed scenes previously read raw `AudioSignal` only (built by `Engine/CosmicEngine.cs`'s `BuildAudioSignal()` directly from `AudioEngine.Guitar1/Guitar2`, no calibration involved) and that `CalibrationEngine`/`InputCalibration` already exposed everything needed — no new state object was built. Fixed a real reliability gap found during investigation: `CalibrationEngine`'s background timer only started lazily via dashboard endpoint calls, so a scene reading it directly would never have started it — converted to a static constructor (CLR-guaranteed on first access to any static member) to fix. `StellarNursery.Render()` and `LavaLampScene.Update()` each now add a capped, modest contribution (`CalibrationEngine.InputA/InputB.CurveOutput * CalibratedBlendWeight` [0.3], clamped to 1.0) on top of their existing raw-audio-derived values — zero diff on either `.frag` shader file, zero effect under silence (`CurveOutput` is 0 at rest). `GET /status` gained `calibratedA`/`calibratedB`/`sceneAudioSource` fields (shown in the dashboard's Scenes tab status bar as "Scene audio source: raw + calibrated blend"); the Calibration tab gained a labeled, auto-clearing "SEND TEST PULSE" button per input (`POST /calibration/testinput`) to prove the integration without real audio connected — the injected value flows through the identical real gain/gate/curve/smoothing/peak pipeline a genuine signal would use, not a shortcut. Verified end-to-end via a full `--dashboard-only` transcript: StellarNursery launched with seed 777 preserved, a test pulse on Input A raised `calibratedA` from 0 to ~0.7 in `/status` within ~2s, switched live to Lava Lamp, a test pulse on Input B raised `calibratedB` to ~0.6 independently of A, clean quit, zero orphan process — plus a live click of the dashboard's Test Pulse button in the browser preview showing the meter fill and TEST badge respond in real time. `dotnet build` 0 warnings/errors; StellarNursery Safe and LavaLamp Safe smoke tests both 75.0 avg fps, no regression. Known limitations: real guitar test of scene response (not just meters) not performed, `CalibratedBlendWeight` is a conservative first-pass value not yet artistically tuned against real playing, calibration values still not persisted, true Clarett multi-channel capture still out of scope, and the same environment-wide screenshot-tooling gap as the two prior passes (substituted with a detailed observed-behavior description plus a full curl transcript). See `AUDIT.md` Entry 27 for full detail, including the reviewer sign-off.
- **Calibration Presets v0.1 (Entry 28): reviewed by ChatGPT as ACCEPTED WITH PRE-COMMIT CORRECTIONS** on 2026-07-12, corrections applied and committed as `bed5e23` (not yet pushed). Adds save/load/delete of named, full-setup calibration presets (routing + both inputs' manual controls and response curves together) to the Calibration tab, directly addressing the user's request after confirming the meters/curves "work great." New `Audio/CalibrationPreset.cs` (data model, capture-from-current, apply-with-validation/clamping/channel-fallback) and `Audio/CalibrationPresetStore.cs` (simple local JSON persistence at `CosmicEngineApp/Config/calibration-presets.json`, not tracked in git, versioned `{"version":1,"presets":[...]}` envelope, corrupt-file quarantine-and-recover, built-in defaults seeded only on first run). New endpoints: `GET /calibration/presets`, `POST /calibration/presets/save|saveas|load|delete`. New dashboard "PRESETS" card at the top of the Calibration tab: dropdown, name field, target-interface selector (Generic/Clarett/Scarlett), Save/Save As New/Load Selected/Delete Selected buttons, and an "UNSAVED" drift-tracking badge. Loading a preset mutates the same live `CalibrationEngine.InputA/InputB` objects both scenes already read every frame, so changes reach a running scene with no restart. Channel-routing safety verified directly: a preset referencing channels unavailable on the current 2-channel backend (simulating a future 8-channel Clarett save loaded on a 2-channel Scarlett setup) loads with routing left unchanged and a specific warning shown, rather than guessing or crashing. Corrupt-file recovery verified directly too — a hand-corrupted preset file was recovered cleanly on restart (moved aside with a timestamp, defaults reseeded, zero crash). Verified: `dotnet build` 0 warnings/errors; StellarNursery Safe and LavaLamp Safe smoke tests both pass; full preset lifecycle (seed defaults → save → change → load exact restoration → channel-fallback warning → delete → invalid-name safety) confirmed via a complete endpoint transcript; both scenes still launch (seed 777 preserved) with calibrated values still reported; clean quit, zero orphan process. Known limitations: true Clarett 8-input capture still not implemented (presets are designed for it, not solving it), real audio not tested (no interface connected in this environment), no cloud sync, no guided auto-calibration, no per-song/setlist preset assignment yet. See `AUDIT.md` Entry 28 for full detail, including the reviewer sign-off.
- **Clarett Multi-Channel Capture Investigation v0.1 (Entry 29, this pass, not yet committed):** investigation-and-recommendation pass (no scene/shader/dashboard-UI changes) answering why the Calibration tab can't yet offer true 8-input Clarett channel selection, and what to do about it. Confirmed the exact ceiling directly on real hardware — a physical Focusrite Clarett ("Clarett 4Pre USB") was connected during this pass. `Audio/AudioEngine.cs` opens capture via `ALC.CaptureOpenDevice(null, 44100, ALFormat.Stereo16, 2048)` using macOS's system `OpenAL.framework` (no bundled OpenAL-soft). A standalone reflection probe confirmed OpenTK's `ALFormat` enum *does* expose multichannel capture formats up to 8 channels (`Multi71Chn16Ext`), but direct testing against the real Clarett showed Stereo16/Mono16 open successfully while MultiQuad16Ext (4ch)/Multi51Chn16Ext (6ch)/Multi71Chn16Ext (8ch) all fail outright (null device handle) — a hard rejection at the native OpenAL/OS capture layer, not a fixable parameter or binding gap. Added low-risk, fully reversible startup diagnostic logging to `AudioEngine.cs` (94 additive lines: available/default/opened device names, requested format, and a 5-format open-then-immediately-close probe) reproducing this finding identically inside the real running app on every future run. Recommended path: a hybrid strategy — keep the existing, already-tested OpenAL stereo path untouched for the Scarlett/live rig, add a new multi-channel-capable backend (PortAudio recommended with moderate confidence over a Mac-only CoreAudio backend, pending confirmation of the OptiPlex's actual OS, which this pass could not establish) specifically for the Clarett/practice rig. Proposed (not implemented) an `IAudioCaptureBackend` abstraction; confirmed during this investigation that `CalibrationPreset.ApplyTo()` (Entry 28) already handles out-of-range channel references safely and the dashboard's channel selectors already read `channelCount`/`channelNames` dynamically — the honest-channel-count plumbing this future work needs already exists, only a real multichannel backend is missing. Verified: `dotnet build` 0 warnings/errors; StellarNursery Safe and LavaLamp Safe smoke tests both pass with diagnostic logging confirmed firing correctly; full dashboard-only regression (Calibration tab still honestly reports 2 channels, presets still intact, both scenes launch with seed 777 preserved, clean quit, zero orphan process). Known limitations: OptiPlex OS unconfirmed, connected Clarett is a 4-input model (not confirmed to match the user's practice-rig unit), no CoreAudio/PortAudio code written yet. See `AUDIT.md` Entry 29 for full detail, including the reviewer sign-off (pending).
- **Wind Turbine Fire Phase 1 v0.1 (Entry 30, this pass, not yet committed):** implemented Phase 1 (v0.1 visual prototype only) of an approved architect plan for a third world, `World03_WindTurbineFire` — a dark, cold-dominant industrial-nightmare tableau (wind-turbine silhouettes against layered smoke, a slow-building ember-glow horizon) that only warms as sustained musical energy accumulates, so heat reads as heat-against-cold contrast rather than orange-on-orange. New `Worlds/World03_WindTurbineFire/WindTurbineFireScene.cs` + `Shaders/wind_turbine_fire.{vert,frag}`, structurally modeled on `LavaLampScene.cs` (relative `ShaderPath()`, `Tuning.*`-based smoothing, the Entry-27 calibrated-additive pattern, a `public static int EmberCount` profile knob). Introduces a continuous (not state-machine) `uSceneHeat` 0-1 accumulator that integrates fire drive over ~240s of sustained loud play and decays at a fixed ~0.01/s during quiet passages. `Engine/SceneRegistry.cs` gained a `WindTurbineFire` entry (Status "Prototype v0.1", Showable, no `ShowSeed`); `Engine/CosmicEngine.cs` gained one profile-knob line at both existing `LavaLampScene.BlobCount` sites. Two real mid-pass defects caught and fixed before being reported as results: a GLSL built-in `noise2()` name collision (compile error, renamed to `vnoise2`) and an inverted vertical orientation (turbines rendered upside-down at the top of frame — root-caused to the codebase's standard `uv.y` flip and fixed with one `p.y = -p.y;`). Heat-progression evidence used a temporary, fully-reverted forced-mock-peak + accelerated-rise-rate build (same precedent as Lava Lamp v0.2's mock-peak probe), confirmed reverted via `grep`. Cold-dominance metric (reusing Lava Lamp v0.2's exact warm/cool-pixel methodology): idle/low-heat capture is 100.00% cold-dominant (0% warm pixels); even a forced-worst-case mock-hot capture stays 81.98% cold-dominant, confirming the readability guard holds. `dotnet build` 0 warnings/errors; WindTurbineFire Safe/High smoke tests 75.1/75.0 avg fps (no regression vs. StellarNursery/LavaLamp Safe, both 75.0 avg fps in the same session); motion diagnostic confirms genuine (if visually modest, by design — a dark/restrained palette) rotor-rotation and ember-drift motion under real silent audio; full dashboard-only transcript (idle → `/scenes` lists the new world → launched as first scene from a fresh dashboard-only start → test pulses on both calibration inputs move `calibratedA`/`calibratedB` independently → live switch to LavaLamp → clean quit → zero orphan process). Zero diff on `World01_StellarNursery/`, `World02_LavaLamp/`, `Audio/*`, `Rendering/*`, `Camera.cs`, `DashboardHost.cs`, `ControlServer.cs`, or any launch script — confirmed via `git status`. Known limitations: Phase 1 scope only (no flame tongues/heat distortion/camera-consume/textures/Blender, all explicitly deferred); no real-guitar validation of the full multi-minute heat timescale; single-run (not perf-sweep) fps readings. See `AUDIT.md` Entry 30 for full detail, including the reviewer sign-off (pending).
- **Wind Turbine Fire Phase 1.1 + 1.2 (Entry 31, this pass, not yet committed):** two small follow-up passes to World03, bundled together per the user's own request after reviewing a Phase 1 screenshot — a shader visual-correctness fix and a scene-specific tuning control, both scoped entirely to `World03_WindTurbineFire/` plus the documented `Tuning.cs`/`ControlServer.cs` extension pattern. **Fix 1 (embedding):** turbine towers now visibly embed in the ground — added a separate constant-radius buried capsule (`EMBED_DEPTH = 0.15`) extending straight down from each tower's existing base point, leaving the visible tapered tower completely unchanged; verified both visually and via a precise per-pixel-column luminance scan at all 4 turbine positions confirming zero gap. **Fix 2 (evolution slider):** new `Tuning.WindTurbineFireEvolutionSeconds` (default 240s, min 30/max 300) live-adjustable via a new dashboard slider in its own clearly-labeled sub-section, following CLAUDE.md's documented `/set`/`/values`/HTML-slider pattern exactly; `WindTurbineFireScene`'s heat rise rate now reads this field every frame (defensively clamped); verified quantitatively via a temporary, fully-reverted debug log showing heat at t=10s of 0.0419 (default 240s) vs. 0.3353 (slider at 30s) — an 8.00x ratio, exactly matching the expected speedup. **Fix 3 (embers repositioned):** embers moved from a full-frame foreground effect with an always-on baseline to a narrow band hugging the horizon glow, gated by the same `fireIntensity` variable that drives the glow band itself (`emberGate = smoothstep(0.05, 0.40, fireIntensity)`, no baseline term), and drawn earlier in the composite so closer elements properly occlude them — a real mid-pass defect (an ember briefly visible in front of a background turbine due to draw order) was caught and fixed before being reported as a result. **Fix 4 (background turbine opacity):** the background turbines' blend-back-toward-background-color factor was cut from 0.60/0.68 to 0.18/0.22, fixing a real transparency bug where the horizon glow showed straight through the silhouette at high heat. `dotnet build` 0 warnings/errors throughout; final smoke tests: WindTurbineFire Safe/High 75.0/75.0 avg fps, StellarNursery Safe 75.0 and LavaLamp Safe 75.1 (regression-checked, unaffected); motion diagnostic under real silent audio confirms motion still present (5.02%/5.33% pixels changed, slightly lower than Phase 1 since embers no longer contribute at rest, expected). Zero diff on `World01_StellarNursery/`, `World02_LavaLamp/`, `Audio/*`, `Rendering/*`, `Camera.cs`, `DashboardHost.cs` — confirmed via `git status`. Known limitations: tuning constants (EMBED_DEPTH, opacity blend factors, emberGate thresholds) are eyeballed against this pass's own screenshots, not validated against real playing or future composition changes; the slider's effect was verified via a temporary debug log rather than a permanent `/status` field. See `AUDIT.md` Entry 31 for full detail, including the reviewer sign-off (pending). **Note: Entry 31 was subsequently reviewed, cleanup corrections applied, and committed as `091da2c` — see the commit-cleanup bullet immediately following this one for that pass.**
- **Wind Turbine Fire Phase 1.1 + 1.2 pre-commit cleanup (committed `091da2c`):** ChatGPT reviewed the Entry 31 evidence package and accepted it with two required corrections, both applied: (1) stale ember comments (from before the Phase 1.2 ember rework) describing embers as having an "always-on baseline"/"glimmering faintly at rest" were corrected in both `WindTurbineFireScene.cs` and `wind_turbine_fire.frag` to accurately describe the current fire-gated-only behavior; (2) `Audio/AudioEngine.cs`'s verbose Clarett multi-format capture-device probe (added in the earlier, separate Entry 29 investigation) was stripped from every normal startup — down to a single minimal "Capture opened: device=..." confirmation line, with the full original findings left preserved as prose in `AUDIT.md` Entry 29. Commit scoped precisely to Wind Turbine Fire work only (`AUDIT.md`, `Audio/AudioEngine.cs`, `ControlServer.cs`, `Engine/CosmicEngine.cs`, `Engine/SceneRegistry.cs`, `Tuning.cs`, the three new `World03_WindTurbineFire/` files, `IMPLEMENTATION_LOG.md`, `PROJECT_STATE.md`, `ROADMAP.md`) — `CLAUDE.md`'s pre-existing, unrelated Entry 29 dirty state was deliberately left out of this commit. Re-verified before commit: `dotnet build` 0 warnings/errors; WindTurbineFire Safe/High, StellarNursery Safe, and LavaLamp Safe smoke tests all pass (~75 fps); full dashboard-only cycle (idle → WindTurbineFire appears in `/scenes` and launches as the first scene from a fresh start, evolution slider present and functional → StellarNursery launches with seed 777 → LavaLamp launches → clean quit → zero orphan process); startup log confirmed no longer cluttered with the verbose probe. Not pushed, per instruction.
- **Wind Turbine Fire Phase 2 (Entry 32, this pass, not yet committed):** fire/smoke improvement pass — shaped flame tongues, better smoke underlighting, masked/capped heat distortion, and a refined fire hue path, all confined to `wind_turbine_fire.frag` (`WindTurbineFireScene.cs` needed zero changes, confirmed via `git diff` — no new uniform required). Flame tongues are 5 domain-warped-FBM shapes masked by an upward-tapering envelope, composited in the same background/horizon region as the existing glow band/embers, gated by `flameGate = smoothstep(0.35, 0.78, fireIntensity)` (reusing the existing gating variable, not a new threshold scheme). Smoke underlighting gained a second, tighter-radius "hot rim" falloff term on top of the original broad ambient tint. Heat distortion is a small, capped screen-space UV displacement masked to a band near the horizon and scaled by `fireIntensity` (zero at rest), applied only to sky/background-turbine/smoke sampling — never to the fire's own light sources or the solid ground/foreground silhouettes. A real mid-pass issue was caught and self-corrected before being reported as a result: the first distortion amplitude (0.016) bent the thin background-turbine silhouettes into a dramatic, "melting"-looking S-curve at high heat; reduced in two steps to 0.0035, re-verified as a restrained shimmer. Readability gate (hard acceptance criterion) passed on the first fully-tuned attempt: cold-dominant percentage at heat≈0.9 moved from 82.41% (before Phase 2) to only 82.33% (after) — the richer visual barely affected overall frame readability. `dotnet build` 0 warnings/errors; motion diagnostic at rest matches Phase 1.2's own baseline almost exactly (no regression to idle turbine rotation/smoke drift); a temporary forced-heat motion diagnostic confirms flame/distortion build smoothly across t1/t5/t15, not strobing; rest-state visual re-check confirms Phase 1.1/1.2 behaviors (embedding, background opacity, ember gating) all still intact. FPS: honest limitation, not a confirmed pass — every smoke test in this session hit the pre-existing, already-documented VSync/window-focus environmental anomaly (Entry 21), confirmed environmental (not Phase-2-specific) by reproducing on LavaLamp/StellarNursery too; frame-time evidence (0.3ms, same cost tier as LavaLamp) suggests no regression but is reported as an inference, not a confirmed ~75fps measurement. Zero diff on `World01_StellarNursery/`, `World02_LavaLamp/`, `Audio/*`, `Rendering/*`, `Camera.cs`, `ControlServer.cs`, `Tuning.cs` — confirmed via `git status`. Two temporary, fully-reverted debug overrides were used for evidence capture (before/after screenshots at fixed heat levels, and a fast-heat-ramp motion check), both confirmed removed via `grep` and a final `git diff` showing zero diff on `WindTurbineFireScene.cs`. See `AUDIT.md` Entry 32 for full detail, including the reviewer sign-off (pending). Not committed, not pushed, per explicit instruction.
- **Wind Turbine Fire Design Correction Pass 1 (Entry 33, this pass, not yet committed):** corrective follow-up to Phase 2 after ChatGPT/user screenshot review flagged two problems — embers reading as big, sparse, close-camera foreground particles, and flame tongues reading as a row of individually repeated cones rather than one continuous distant fire front. Fix scoped to `wind_turbine_fire.frag` plus a 1-line-pattern ember-count change (Safe 12→28, High 24→40) at both `Engine/CosmicEngine.cs` call sites. Embers: smaller (squared-hash size skew), clustered near 4 hash-derived fire-front locations instead of uniform full-width spawn, a per-ember brightness power curve so most land dim, a smaller halo, base-weighted vertical bias, a cooling/shrinking cue as they age, ~20% slower drift. Flame tongues: the 5 discrete evenly-spaced tongues are deleted and replaced with one continuous scrolling height-field fire front (warped-x FBM height function, soft wide-topped mask, scrolling-FBM "licks" texture), fused into the existing horizon glow band as a single additive term instead of two stacked layers, with distance contrast inverted (hot at the base, hazing toward the tip, rather than Phase 2's tip-whitening). Smoke: the horizon band itself and a new near-horizon smoke veil are both broken up with FBM so the fire visibly blends into the haze rather than sitting as a crisp cutout. `flameGate`/`emberGate` thresholds, heat-distortion scope, background-turbine opacity, turbine embedding, and composite order are all unchanged. Readability gate (reused Phase 2's exact methodology) passed on the first attempt — warm-pixel percentage at heat≈0.9 actually went slightly *down* (17.67%→17.04%) despite ~2.3x more embers and continuous fire-front coverage. `dotnet build` 0 warnings/errors; all four required smoke tests clean and vsync-paced this session (WindTurbineFire Safe/High ~74/75fps, StellarNursery Safe seed 777 ~75fps, LavaLamp Safe ~75fps); motion diagnostic under forced heat confirms genuine (not frozen/strobing) fire/ember/turbine animation (25%+ pixels changed T1→T5/T5→T15); full dashboard-only transcript (idle, no auto-launch → `/scenes` lists WindTurbineFire → launches, fire-evolution slider present → StellarNursery launches with seed 777 → LavaLamp launches → clean quit → zero orphan process, `pgrep`/`lsof` both clean). A temporary `TempForcedHeat` debug override (same precedent as Phase 2's own technique) was used for deterministic before/after screenshot capture, then fully reverted — confirmed via `grep` and a final `git diff` showing zero diff on `WindTurbineFireScene.cs`. Known limitations: no true volumetric smoke, heat distortion already existed from Phase 2 (untouched, not new), no camera-consuming fire/Blender assets, not final fire art, no OptiPlex/show-hardware validation, all new tuning constants eyeballed against forced-heat screenshots rather than a real sustained heat ramp. See `AUDIT.md` Entry 33 for full detail, including the reviewer sign-off (pending). Not committed, not pushed, per explicit instruction. **Note: Entry 33 was subsequently reviewed by the user and accepted with no corrections requested, and committed as `2a35904` — see the Refinement Pass 2 bullet immediately following this one for the follow-up pass built on top of that commit.**
- **Wind Turbine Fire Refinement Pass 2 (Entry 34, this pass, not yet committed):** three small, controlled refinements on top of the accepted/committed Design Correction Pass 1, per explicit user request to preserve the current design rather than redesign it. (1) Heat-wave distortion made distant turbines wobble/cartoon-like at high intensity — root-caused to the same high-frequency spatial distortion wave being sampled at different points across one turbine's thin silhouette (tip vs. base), causing independent-looking bending; fixed with a separate, gentler (~35% amplitude), lower-frequency (~1/3), height-tapered warp used only for background-turbine SDF sampling, plus a soft-clamped overall intensity drive (max 0.80) and a ~20% base amplitude cut — sky/smoke shimmer amplitude/frequency is otherwise unchanged. (2) Ember density now climbs progressively with fire intensity via a new per-ember soft activation threshold (new hash offset `seed+8.0`), while every existing size/placement/clustering/brightness-curve/motion rule is completely untouched, per explicit request to preserve the current ember look. (3) The fire front's height cap now keeps growing past where `flameGate` itself saturates (was flat once saturated), and a new separate high-altitude haze layer (soft, FBM-modulated, gated to mid-high intensity only) extends the glow further into the sky without the fire front's own crisp mask growing tall enough to read as flame geometry. No shared engine file was touched this pass — the entire diff is confined to `wind_turbine_fire.frag` (confirmed via `git diff --stat` against `SceneRegistry.cs`/`CosmicEngine.cs`/`ControlServer.cs`/`Tuning.cs`, all zero) — per the user's own note on scope-appropriate testing, the StellarNursery/LavaLamp checks below were run because explicitly requested for this pass, not because a shared file was at risk. `dotnet build` 0 warnings/errors; all four required smoke tests clean and vsync-paced (WindTurbineFire Safe/High ~75fps, StellarNursery Safe seed 777 ~75fps, LavaLamp Safe ~75fps); full dashboard-only transcript (idle/no-auto-launch → scene list → launch → evolution slider present → StellarNursery seed 777 → LavaLamp → clean quit → zero orphan process); readability sanity check (not a required gate this pass) showed warm-pixel percentage barely moved (17.05%→17.38% at heat 0.9) despite the taller fire front and new haze layer, confirming no readability regression. Honest limitation: the distant-turbine-wobble fix is well-reasoned and verifiably reduces distortion magnitude by the numbers, but a rigorous before/after static-frame comparison (swapping in the pre-refinement committed shader, matched motion-diagnostic timestamps/rotor angles) could not visually confirm a dramatic before/after swing at this pass's own screenshot resolution — the original complaint was about a live/animated view, which stills can't fully reproduce; flagged honestly rather than asserted as a fully confirmed visual pass. A temporary `TempForcedHeat` debug override (same precedent as prior passes) was used for deterministic capture, then fully reverted — confirmed via `grep` and a final `git diff` showing zero diff on `WindTurbineFireScene.cs`. Known limitations: no true volumetric smoke, no camera-consuming fire/Blender assets, not final fire art, no OptiPlex/show-hardware validation, real audio/song-length heat evolution still needs hands-on tuning. Recommended next action: Phase 3 (turbine/geometry improvement) — fire/smoke has now had three dedicated passes. See `AUDIT.md` Entry 34 for full detail, including the reviewer sign-off (pending). **Note: subsequently committed together with the Entry 38 investigation as `1615b12` — see the Phase 3 bullet below for the next pass.**
- **Wind Turbine Fire "no fire / no audio reaction" investigation (Entry 38, committed `1615b12`):** live reproduction found zero code defect — root-caused to this machine's macOS default audio input being "Hue Sync Audio" (a virtual device), not a real guitar interface; see the header note above and `AUDIT.md` Entry 38 for full detail. Documentation-only, no source file changed in the final state.
- **Wind Turbine Fire Phase 3 (Entry 39, committed `81e58fa`):** turbine/geometry de-stiffening pass — the next phase of the originally-approved Fable plan, now that fire/smoke (Phase 2, Design Correction Pass 1, Refinement Pass 2) is committed. Scoped entirely to `wind_turbine_fire.frag` (`turbineSDF()` renamed `turbineMask()`, now returning `vec2(mask, nacelleShade)` instead of a raw distance so blade motion-blur can average thresholded masks rather than union raw distances) — zero diff on `WindTurbineFireScene.cs` or any shared engine file, confirmed via `git diff --stat`. (1) Blade motion blur: 3 angle samples around the current rotor angle, averaged, with blur half-angle driven by `uWindDrive` (already an existing uniform — none added) via GLSL consts mirroring the C# rotor-speed constants; ramps in only above a wind-drive threshold so idle blades stay crisp. (2) Tower flex: the tower is now a 3-segment tapered-capsule chain that picks up a small height-increasing horizontal offset plus a slow per-turbine sway at high wind, returning fully upright at low wind; nacelle/blades follow the flexed hub. (3) Nacelle detail: a secondary capsule (tail/generator-housing stub) unioned onto the nacelle, plus a subtle top-lit/underside-shaded tint local to its own footprint, foreground turbines only — this was built and initially shipped in this pass, but a same-day rendering sliver artifact (highlight bleeding past the silhouette edge) was found and fixed, and then the whole nacelle-detail feature was removed entirely per user feedback ("looks cheap"); the nacelle is a plain flat-black capsule, unchanged from before this phase (see `AUDIT.md` Entry 39 addenda). (4)/(5) Parallax + haze grading: the farther background turbine (BG2) nudged smaller/higher/hazier relative to the nearer one (BG1), widening a previously narrow (0.04) haze-blend gap to 0.16, still well under the 0.60/0.68 Phase 1.2 fixed as a real transparency bug. Verified mathematically neutral at rest by construction (not just visually) — see `AUDIT.md` Entry 39. `dotnet build` 0 warnings/errors; WindTurbineFire Safe avg 75.0fps/min 74.9, High avg 75.1fps/min 74.9 — no measurable regression. Before/after screenshots at a temporary forced `uWindDrive=0.90` (`TEMPPHASE3CAPTURE`-marked, fully reverted — confirmed via `grep` and a final `git diff` on `WindTurbineFireScene.cs` showing zero diff) clearly show blade blur, tower lean, and nacelle detail on both foreground turbines, and improved background-turbine depth separation; a `git show HEAD:...`-swapped pre-Phase-3 shader was used for the "before" side. Motion diagnostic at real (non-forced) audio: 4.76%/5.15% pixels changed T1→T5/T5→T15, consistent with prior documented baselines — turbines still visibly rotate correctly, nothing broken/glitchy. Horizon/fire-band crops from the same forced-wind captures are visually identical before/after, confirming zero regression to fire/glow (untouched this pass). Known limitations: the 3-sample blur technique can read as several discrete "ghost" blade positions at high wind rather than a perfectly smooth blur (inherent to the cheap single-pass approximation, not a bug); tower-flex/nacelle-shade constants are eyeballed against this pass's own screenshots, not a real sustained wind/audio session; no Blender/mesh work performed (deferred to a future review cycle only if still needed). See `AUDIT.md` Entry 39 for full detail, including the reviewer sign-off (pending). **Committed as `81e58fa`.**
- **Mac AMFI Apphost Workaround Pass (Entry 37, this pass, not yet committed):** infrastructure/launcher pass, zero shader/scene-art changes — resolves the Entry 35/36 startup blocker without any machine-wide security-setting change. Confirmed the failure once more, bounded: `dotnet run -- --smoke-test` still exits 137 (SIGKILL) with zero output, and `log show` still shows the same `AppleSystemPolicy: Security policy would not allow process` denial against the locally-built apphost. Fix: `CosmicEngine.App.csproj` gained `<UseAppHost>false</UseAppHost>` (stops generating the ad-hoc-signed apphost entirely; as a direct, verified side effect `dotnet run` itself started working again, since it now execs via the trusted `dotnet` host rather than the apphost), and `run-show.sh`/`Run Cosmic Engine.command` were changed to build once (`dotnet build`) then launch the built `.dll` directly (`dotnet bin/Debug/net8.0/CosmicEngine.App.dll --dashboard-only`) rather than `dotnet run` — the more robust fix, since it doesn't depend on `UseAppHost` staying set. Verified: build clean on both copies; DLL-host smoke tests pass for StellarNursery Safe (seed 777), LavaLamp Safe, WindTurbineFire Safe (world exists on this branch as uncommitted work from an unrelated pass — used only as a valid `--world` test value, its source files were not touched), and a High-profile run, all ~74-75 fps; full `--dashboard-only` cycle exercised directly via the dashboard's own HTTP API (idle status → `/scenes` lists all three worlds → `POST /launch` starts a scene live → `POST /quit` stops it) with zero orphan process and port 8080 free after every single run, checked via `ps`/`lsof` before and after each command, not just at the end; the full `run-show.sh` launcher was run end-to-end (build → dashboard up → scene launched from dashboard → quit) with the same zero-orphan result. Both the external-drive and internal-disk copies updated and confirmed byte-identical via `diff` for every changed file (`CosmicEngine.App.csproj`, `run-show.sh`, `CLAUDE.md`); `Run Cosmic Engine.command` needed no changes since it only `exec`s `run-show.sh`. No machine-wide security setting was touched — Gatekeeper was not disabled, PACE Eden/`licenseDaemon` was not stopped, Startup Security Utility/Recovery Mode was not touched, no `sudo` was run. Known limitations: not tested on a machine where Developer Mode is disabled and never was (this fix's mechanism — avoiding the apphost — should be orthogonal to that setting, but that combination wasn't directly observed this pass); a full post-reboot re-verification (in case the Entry 36 reboot theory was itself also a contributing factor) was not performed, since the workaround already succeeds without it. See `AUDIT.md` Entry 37 for full detail, including the reviewer sign-off (pending, not self-signed).
- **Underwater Phase 1 v0.1 (Entry 40, committed `5f30ec1`):** implemented Phase 1 (atmosphere prototype only — no jellyfish, tentacles, silhouettes, or refraction warp, all deferred) of an approved architect plan for a fourth world, `World04_Underwater` — a dark, cool three-zone water column (dim green-teal light entry, desaturated slate-blue midwater, near-black blue-violet abyss) lit by slow-swaying analytic god rays, upper-water caustic shimmer, drifting haze/murk, and marine-snow particulate that glints as it drifts through the light shafts. New `Worlds/World04_Underwater/UnderwaterScene.cs` + `Shaders/underwater.{vert,frag}`, structurally modeled on `WindTurbineFireScene.cs`/`wind_turbine_fire.frag` (relative `ShaderPath()`, `Tuning.*`-based smoothing at 0.60 vs. WTF's 0.40 for a more languid response, the Entry-27 calibrated-additive pattern, a continuous `_bloom` 0-1 accumulator copied mechanically from `_sceneHeat`'s mechanics, `public static int ParticleCount` profile knob). Guitar 1/Input A ("Creator") drives light (ray/caustic/glow brightness, bloom accumulation) through an added asymmetric attack/release envelope (~0.5s attack, ~2.6s release) so light swells and lingers rather than twitching; Guitar 2/Input B ("Sculptor") drives current speed/particle drift, with treble2 adding transient turbulence mirroring WTF's `smokeTurbulence` shape. `Engine/SceneRegistry.cs` gained an `Underwater` entry (Status "Prototype v0.1", Showable, no `ShowSeed`); `Engine/CosmicEngine.cs` gained one profile-knob line (`ParticleCount`, Safe 36/High 56) at both existing `WindTurbineFireScene.EmberCount` sites; `Tuning.cs` gained `UnderwaterEvolutionSeconds` (default 240s) and `ControlServer.cs` gained the matching `/set`/`/values`/HTML-slider wiring, mirroring `WindTurbineFireEvolutionSeconds` end-to-end including its mm:ss display helper. **One real mid-pass defect caught and fixed before being reported as a result:** the first shader draft's god rays/caustics/particles were all far too dim to read as underwater in the honest rest-state screenshot — root-caused to the ray origin sitting too far above the visible frame (`p.y=0.78`) so the exponential depth-attenuation had already eaten most of a ray's brightness before it even entered frame, compounded by marine-snow particle sizes small enough to be sub-pixel at 1280x720. Fixed by moving the ray origin to `p.y=0.60` (just above the visible top), widening the ray band and raising ray/caustic/particle brightness multipliers, and roughly tripling particle size — re-verified with a fresh rest-state capture showing clearly visible, swaying god rays with particles glinting inside them. Bloom-progression evidence (0.1/0.5/1.0) used a temporary, fully-reverted `TEMPMOCKBLOOMCAPTURE` override (forced `_bloom`/`_lightEnvelope`, same precedent as WTF's `TempForcedHeat`), and the evolution-slider ratio check used a separate, also fully-reverted `TEMPEVOSLIDERCHECK` override (forced full light drive + a console bloom log) — both confirmed removed via `grep -c` returning 0 on every touched file plus a clean rebuild. Evolution-slider verification: bloom at t=10s was 0.0396 at the default 240s setting vs. 0.3169 at 30s — an 8.00x ratio, exactly matching the expected 240/30 speedup (same methodology as Entry 31's WindTurbineFireEvolutionSeconds precedent). Warm-pixel/cold-dominance metric (reusing the established `compute_metrics.py` script unchanged): **0.00% warm pixels in both the rest-state and the forced-mock-bloom=1.0 capture** — 100.00% cold-dominant in both, confirming this is a genuinely all-cool-palette scene, unlike Wind Turbine Fire. `dotnet build` 0 warnings/errors throughout; Underwater Safe/High smoke tests 74.9/74.9 avg fps (no regression vs. StellarNursery/LavaLamp/WindTurbineFire Safe, all ~75 avg fps in the same session); motion diagnostic under real silent audio shows 7.35%/12.59% pixels changed T1→T5/T5→T15 (ray sway, particle drift, caustic scroll — genuine motion, not frozen/strobing); full dashboard-only transcript (idle → `/scenes` lists the new world → launched as first scene from a fresh dashboard-only start → test pulses on both calibration inputs move `calibratedA`/`calibratedB` independently → live switch to LavaLamp → clean quit → zero orphan process). A pre-existing orphaned `--dashboard-only` process (unrelated to this pass, started before this session began) was found squatting on port 8080 at the very start of testing and was cleaned up so bounded tests could run; noted honestly rather than silently worked around. Zero diff on `World01_StellarNursery/`, `World02_LavaLamp/`, `World03_WindTurbineFire/`, `Audio/*`, `Rendering/*`, `Camera.cs`, `DashboardHost.cs`, launch scripts, or the `.csproj` — confirmed via `git status`/`git diff`. Known limitations: Phase 1 scope only (no jellyfish/tentacles/silhouettes/refraction, all explicitly deferred to a future phase); no real-guitar validation of the full multi-minute bloom timescale; single-run (not perf-sweep) fps readings; all brightness/size tuning constants are a first-pass eyeball tuning against this pass's own screenshots, not validated against real sustained playing. See `AUDIT.md` Entry 40 for full detail, including the reviewer sign-off (pending, left blank). **Note:
subsequently committed as `5f30ec1`** (`CLAUDE.md`'s unrelated Entry 29 Clarett dirty content was excluded
from that commit via the same targeted line-level staging technique as the Entry 31/38 precedents) — see
the Phase 2 bullet immediately below for the next pass built on top of that commit.
- **Underwater Phase 2 v0.1 (Entry 41, this pass, not yet committed):** jellyfish/tentacle pass on top of
  the now-committed Phase 1 atmosphere — 3 mid-distance jellyfish, SDF bell (dome+skirt smooth-union,
  domain-warped rim), an asymmetric contract-fast/relax-slow pulse that reshapes the SDF itself (C#-
  integrated phase, mirroring WindTurbineFireScene's rotor-angle pattern since pulse rate is audio-
  reactive), a continuous ridged-FBM tentacle filament field (explicitly not discrete tentacle curves,
  applying Entry 33's lesson), controlled 35-50% bell-interior translucency, and rim-weighted
  bioluminescence (Input A brightness, Input B pulse rate, no static ornamental highlight per Entry 39's
  nacelle lesson). Scoped entirely to `UnderwaterScene.cs`/`underwater.frag` — zero diff on every other
  world or shared engine/audio/rendering file. One self-caught design-correction iteration: the first
  draft read as "glowing orb with strings" (interior/tentacles too faint against the background) and was
  fixed with a brighter/radial-glow body and higher-contrast tentacle brightness before being reported.
  `dotnet build` 0 warnings/errors; Safe/High smoke tests 75.1/75.0 avg fps (matches Phase 1's own
  74.9/74.9 baseline); motion diagnostic 13.60%/20.08% pixels changed T1→T5/T5→T15 (up from Phase 1's
  7.35%/12.59%, consistent with genuine added jellyfish motion); warm-pixel/cold-dominance metric
  unchanged at 0.00% warm / 100.00% cold-dominant at both rest and forced bloom=1.0 — no readability
  regression. See `AUDIT.md` Entry 41 for full detail, including the reviewer sign-off (pending, left
  blank). **Not committed, not pushed, per explicit instruction — leave uncommitted for its own review.**
- **Jellyfish Tentacle Rescue Pass 1 (Entry 41 addendum 5, this pass, not yet committed):** the sixth round
  of tentacle-specific work — the prior five (base noise field, per-strand fix, frequency retune, discrete
  capsule-chain pivot, Bezier-SDF rewrite) all failed user review; the user was considering scratching the
  jellyfish design entirely unless this pass fixed it. Root cause of round 5's rejection ("straight
  spikes"/"laser rays"/"pivot around attachment points"): a single quadratic Bezier per tentacle has exactly
  one control point between root and tip, so swinging it rotates the whole strand as one rigid shape hinged
  at the root — a smooth curve, but rigid-body motion. Fixed with a genuine traveling-wave technique: each
  tentacle sampled at 16 points along its length, lateral offset =
  `amplitude(t) * sin(t*waveFreq - uTime*waveSpeed + phase)` (multiple bends distributed along the length
  that travel down the strand over time, not one bow swinging in place), amplitude/static-bend/current-drift
  all growing with `t` so the root stays anchored and the tip trails/curls freely. Samples connect into a
  smooth-min-blended capsule polyline (`sdCapsuleT`, replacing round 5's now-removed `sdBezierT`). New
  `centerBias` gives central attach points a shorter/thicker (oral-arm-like) character vs. longer/thinner
  outer marginal tentacles. Honest performance tradeoff: `tentacleCount` reduced 14-24→10-18 after a
  High-profile fps measurement (Bezier baseline 74.4fps → 41.2fps before the retune → 49.5fps after);
  Safe profile (this project's live/stage-safety profile) showed no fps impact throughout. Scoped entirely
  to `underwater.frag` (tentacle section rewrite + `sdCapsuleT` helper) — `UnderwaterScene.cs` touched only
  for a temporary, fully-reverted debug capture override (`TempTentacleFixCapture5`, confirmed removed).
  Zero diff on every other world or shared engine/audio/rendering file. Before/after full-frame screenshot
  comparison shows an unambiguous, categorical difference — see
  `DiagnosticReports/JellyfishTentacleRescue01_20260714_173902/` (zipped) for the full evidence package,
  including a `before_after_tentacle_side_by_side.png` direct comparison. Honest verdict: jellyfish design
  rescued (this is the first round to change the tentacle *motion model*, not just its *shape
  representation*). See `AUDIT.md` Entry 41 addendum 5 for full detail, including the reviewer sign-off
  (pending, left blank). **Note: subsequently committed as `bffc834`** (alongside the tentacle
  performance follow-up below), the same session that also committed Entry 42's ControlServer fix as
  `bf5c8f6`.
- **Tentacle Rescue Pass 1 performance follow-up (Entry 41 addendum 6, committed `bffc834`):** a pure
  performance pass on the just-accepted traveling-wave tentacle rewrite — a per-tentacle early-out
  (analytically-derived worst-case reach check) plus a tightened per-jellyfish bounding box recovered
  High-profile fps from a freshly-measured 5-run baseline of 48.6fps to a final 5-run baseline of
  68.78fps average (crossing the user's explicit 60fps requirement), with a pixel-diff confirming the
  optimizations are visually indistinguishable (correctness-preserving prunes, not approximations). See
  `AUDIT.md` Entry 41 addendum 6 for full detail.
- **ControlServer dashboard startup reliability fix (Entry 42, committed `bf5c8f6`):** unrelated
  infrastructure fix, investigated after a real crash report (`HttpListener.Start()` unhandled exception
  on macOS's managed HttpListener implementation under port contention). Added retry-with-backoff and a
  clear actionable error message. Scoped entirely to `ControlServer.cs`'s `Start()` method — see
  `AUDIT.md` Entry 42 for full detail.
- **Underwater Phase 3 v0.1 (Entry 43, committed `bfcf82a`):** caustic/ray interaction polish
  (ray edge softening via a wider/dimmer halo Gaussian, a softer depth-attenuation curve, and a sharpened
  power-curve concentration of caustic energy specifically inside ray interiors rather than a flat
  upper-water band) plus a subtle, always-on foreground refraction warp on the water gradient/rays/
  caustics/haze layers only — jellyfish/tentacles are rendered against the original unwarped coordinate,
  fully excluded from the warp (not a tapered partial warp), per this project's own two-tier heat-
  distortion lesson (Wind Turbine Fire, Entry 34) applied at its most conservative end to protect the
  just-rescued six-round tentacle work. Depth-framing silhouettes (optional item 3) were evaluated
  against screenshots and deliberately not added — the composition already reads well without them.
  Scoped entirely to `underwater.frag`; `UnderwaterScene.cs` touched only for a temporary, fully-reverted
  debug override (`TempPhase3RefractionCapture`, confirmed removed) — no permanent C#-side change, no new
  uniform (refraction amplitude derives from existing `uCurrentDrive`/`uCurrentTurbulence`). `dotnet
  build` 0 warnings/errors; High-profile fps held at 68.3-68.4 avg across 3 runs (at/near the Entry 41
  addendum 6 floor of 68.78fps, no regression); Safe unaffected (74.9fps). Rest-state luminance 0.073
  (prior range 0.063-0.071 — small, expected increase, not a wash-out). A pixel-diff crop specifically
  isolating a jellyfish confirms no wobble/distortion (0.32% of pixels differing >5/255, consistent with
  ordinary run-to-run timing jitter, not a code effect) — backed by the architectural guarantee that
  `renderJelly()` and its three call sites never reference the warped coordinate (confirmed via `grep`).
  See `AUDIT.md` Entry 43 for full detail, including the reviewer sign-off (pending, left blank).
  **Committed as `bfcf82a`** (Phase 0 of the Phase 1 "Living Water" session below), not pushed.
- **Underwater Phase 1 "Living Water" v0.1 (Entry 44, committed `e1c576b`):** converts the
  previously screen-fixed jellyfish (Phase 2) into drifting inhabitants of an evolving environment, directly
  answering the user's "jellyfish just sit there" complaint. Jellyfish position/depth is now C#-integrated
  drift-path state (`UnderwaterScene.cs`'s `JellyDriftState`) instead of a static hash — each jellyfish
  crosses the frame edge-to-edge over ~90-150s (current-biased, depth-biased speed), wrapping to a freshly
  re-hashed depth/vertical-wander shape each crossing, staggered so at most one is ever fully off-frame at
  once (verified by construction). Depth now drives scale (0.50-1.15x)/haze-occlusion/speed. The previously
  fully-unused `Camera` class is now wired up as a per-layer parallax shift (water 0.2x, rays/caustics 0.5x,
  haze 0.7x, particles/plankton/jellyfish 1.0x). A new bioluminescent plankton bloom-field layer (cheap
  point/glow, distinct from marine snow) supersedes the old "foreshadowing only" bioluminescent-motes stub,
  hard-gated by named bloom-arc bands (Deep Calm/Bioluminescent Awakening/Current Build/Bloom Event) that
  now drive plankton density/brightness/streaming character plus a shared current-drive lift across haze/
  marine-snow/plankton. `SceneRegistry.cs`'s `Underwater` entry renamed to "Abyssal Bloom" (display only,
  `Id` unchanged). Mandatory translation-invariance regression check passed (architectural guarantee + a
  matched-condition capture pair, residual diff traced to ordinary sub-pixel anti-aliasing on thin tentacle
  lines, not shape distortion). Performance: found and verified (via controlled `git stash` A/B, not
  assumed) a real, code-attributable High-profile improvement over the Phase-0 baseline (67.7-67.9fps →
  74.7-75.0fps final, 5-run); also found, investigated, and honestly disclosed a synthetic worst-case
  boundary (~60-62fps under an adversarial forced-clustering scenario) without chasing further optimization
  once realistic operation was confirmed comfortably clear of the 60fps floor. See `AUDIT.md` Entry 44 for
  full detail, including the reviewer sign-off (pending, left blank).
- **Underwater Phase 2 "Bloom refinement" v0.1 (Entry 45, this pass, not yet committed):** gives the Phase 1
  plankton bloom field real structure instead of independent per-mote random drift/twinkle. Flow-field
  alignment: plankton are grouped into 7 coarse, smoothly-blended "current channels" (mirroring the
  jellyfish-tentacle lane-blending technique, Entry 41 addendum 1), each with its own slowly-evolving flow
  direction that biases plankton motion — verified analytically (a Python re-implementation of the exact
  shader math) to show within-channel flow-angle alignment averaging 0.77-0.85 (near 1.0 = tightly aligned
  stream) versus only 0.37-0.38 across different channels' means. Pulse-train brightness waves: a literal
  traveling-wave function of position and time replaces the old per-plankton independent blink, verified
  analytically to be a true traveling wave (floating-point-precision invariance along the wave's own travel
  line). Both mechanics' strength/frequency rise with the existing bloom-arc's `bloomNorm`, plus a new
  brightness-variance widening term, giving Awakening→Bloom Event a genuine qualitative escalation, not just
  more/brighter dots. **A real performance regression was found by this pass's own mandatory check and
  fixed, not just accepted:** the first implementation cost ~17% relative fps at forced worst-case (all
  plankton, full Bloom Event) versus a contemporaneous Phase-1-only baseline; two optimization rounds
  (hoisting a per-pixel-redundant per-plankton computation into a once-per-pixel array, dropping unused
  transcendental calls) brought that down to ~5% relative. Also found and honestly disclosed (not silently
  accepted) that Phase 1's own pre-existing plankton-loop cost already sits below the 60fps floor at that
  same forced full-density scenario — unlike a prior disclosed worst case (Entry 44's clustered-jellyfish
  edge), this is the scene's designed climax state, not a rare synthetic edge, so it's flagged as a genuine
  follow-up candidate rather than swept under the rug. Confined entirely to `underwater.frag`'s plankton
  section — `UnderwaterScene.cs` ends this pass with zero diff (no C#-integrated state was needed; flow/pulse
  timing reads `uTime`/`bloomNorm` directly rather than requiring audio-rate integration). Zero regression to
  jellyfish drift/pulse/tentacles, camera parallax, marine-snow particles, water/ray/caustic/haze layers, or
  the bloom-arc band boundaries (all confirmed unchanged). See `AUDIT.md` Entry 45 for full detail, including
  the reviewer sign-off (pending, left blank).
  **Not committed, not pushed, per explicit instruction — leave uncommitted for its own review.**
- **Underwater Phase 2 "Bloom refinement" perf-fix addendum (Entry 45 addendum, this pass, not yet
  committed):** priority follow-up treating the base pass's own disclosed sub-60fps shortfall at forced full
  Bloom Event (High, 50.5-50.7fps) as a priority item. Isolated the plankton loop as the sole cause (disabling
  it recovered fps to rest-state levels), then found it had no spatial early-out at all — every fragment paid
  full per-plankton cost regardless of that plankton's ~0.001-0.003 p-unit visible radius. Fixed via a
  mathematically exact per-frame reach bound (not a headroom guess) plus a small prefix-cost hash
  consolidation, in two measured rounds. **Forced Bloom Event, High: 49.9-50.1fps → 65.1-65.4fps avg (5
  runs, min observed 64.5-64.8)** — a ~30% relative improvement, crossing the 60fps floor with real margin.
  Visual preservation confirmed via git-stash A/B: `--diagnostic visual` at forced `uBloom=0.85` measured
  pixel-identical luminance (0.137/0.139/0.141) between the pre-fix and post-fix code. Confined entirely to
  `underwater.frag`'s plankton section; `UnderwaterScene.cs` ends this addendum with zero diff (temporary
  debug overrides fully reverted). See `AUDIT.md` Entry 45 addendum for full detail, including the reviewer
  sign-off (pending, left blank).
  **Not committed, not pushed, per explicit instruction — leave uncommitted for its own review.**

- **Abyssal Bloom Phase 3 "Distant Event" v0.1 (Entry 46, committed `fed7a7d`):** a fresh,
  ChatGPT-specified phase addressing the review that the scene "still mostly reads as jellyfish under light
  rays, with plankton as a supporting layer" — adds one new environmental element, a distant, irregular,
  domain-warped field of abyssal glow low in the water column, gated across the same `uBloom` arc every other
  layer already uses (`glowArc = pow(uBloom, 2.3)`, near-zero through Deep Calm, fullest at Bloom Event) and
  spatially masked to the bottom ~35-40% of frame. Two design corrections were self-caught before reporting
  via an isolated-render diagnostic technique (rendering only this layer's own output with the rest of the
  pipeline bypassed): a wrong assumption about `fbm2()`'s output range made the layer far too dim to see at
  any bloom level (fixed via explicit renormalization), and the vertical mask's own smooth gradient initially
  dominated the noise field's horizontal variation, reading as flat bands rather than an irregular field
  (fixed via a perturbed mask boundary, higher macro frequency for 2-3 visible lobes, and rebalanced
  ambient/core weighting). Isolated, deconfounded emergence evidence shows a genuine ~100x monotonic
  brightness increase from bloom 0.10 to 0.90 in a fixed bottom-band sample. A first implementation measured
  61.0-61.4fps at forced Bloom Event (High) — above the 60fps floor but with thin margin against this
  project's own documented session-level fps variance — optimized (fbm2 octave reduction, tighter vertical-
  mask footprint) to a final 63.6-65.0fps (steady-state 64.8-65.0fps), within noise of the established
  65.1-65.4fps baseline. Confined entirely to `underwater.frag`; `UnderwaterScene.cs` ends this pass with
  zero diff (temporary debug overrides fully reverted). Jellyfish/tentacle/plankton/rays/caustics/haze/
  water-gradient code all confirmed byte-identical via diff, and visually unchanged (rest-state luminance
  0.066-0.067, matching the established 0.063-0.073 range; motion diagnostic 16.65%/24.33%, matching the
  established 16.47%/24.11% baseline). See `AUDIT.md` Entry 46 for full detail, including the reviewer
  sign-off (pending, left blank).
  **Committed as `fed7a7d` ("Add Abyssal Bloom distant glow event").**
- **Abyssal Bloom Phase 4 "Presence / Color / Depth Population" v0.1 (Entry 47, this pass, not yet
  committed):** direct user-review feedback superseding ChatGPT's originally-recommended "Song
  Feel/Audio Tuning" phase — "three jellyfish floating around is not enough interest for a full song," too
  sparse/monochromatic. Three additions, all confined to `underwater.frag` plus a `BackgroundJellyCount`
  profile-scaled knob (`UnderwaterScene.cs`/`CosmicEngine.cs`): (1) a distant, abstract "Distant Alien
  Presence" — a large, anisotropic, irregular soft mass composited as a **darkening** mix (not additive
  brightening, unlike every other layer, chosen specifically to avoid a "pasted silhouette" read), gated by
  its own independent "appears every few seconds" `uTime`-only pulse cycle layered on top of the existing
  `uBloom` arc; (2) six small, mostly zero-extra-cost color-variation nudges (caustic green/blue drift, haze
  violet/indigo shadow nudge, a pulsing magenta accent on the glow field's own existing violet nudge, a
  cool-hue ray drift, a rare warm plankton spark, a blue-violet background-jellyfish palette); (3) 4-8
  cheap, reduced-detail background jellyfish (`hash1`/`sin`/`exp` only, no `fbm2`, no tentacle field, own
  0.82x parallax tier) via pure shader-side hash+`uTime` placement (no C#-integrated drift state needed).
  **One self-caught design correction** (governance rule 10 budget not exhausted): the presence layer's
  first implementation domain-warped world-space noise at too low a frequency relative to its own footprint,
  reading as a smooth "eel/leaf" shape on isolated-render review — rebuilt around shape-local-frequency noise
  (a "soft gate bounding an irregular patchy field" architecture) that reads as genuinely amorphous/irregular
  on re-verification. Performance: High stayed ≥60fps in all three required forced states across 10+ runs
  (Deep Calm 77.7-79.6fps; High Bloom 61.4-62.6fps; Monster/Presence Peak — forced bloom AND forced pulse
  simultaneously — 62.2-62.6fps), a real but modest (~2-4fps) margin versus Phase 3's own ~65fps floor,
  disclosed honestly with an isolation breakdown rather than hidden. `Engine/CosmicEngine.cs` touched (one
  profile-knob line) so, per the user's own scoped-regression preference, StellarNursery/LavaLamp/
  WindTurbineFire were re-run this pass — all pass cleanly. See `AUDIT.md` Entry 47 for full detail,
  including the reviewer sign-off (pending, left blank).
  **Not committed, not pushed, per explicit instruction — leave uncommitted for its own review.**

## P1 Acceptance Criteria (RenderScale + Live/Safe Profiles + FPS Variance Evidence) — STATUS: implemented, evidence gathered, awaiting review

- [x] `RenderScale` exists and affects the actual render-target resolution (not just window/blit scale). — confirmed: `Safe` measured at a real 640x360 target, `High` at 1280x720.
- [x] At least two named profiles exist: `Safe` and `High` (or `Art`). — `Safe`, `Balanced` (bonus), `High` all implemented.
- [x] The currently active profile is logged in `[Perf]`.
- [x] The actual render target size is logged in `[Perf]`.
- [x] `Safe` profile is measurably cheaper (lower render cost / higher FPS) than `High`/`Art`. — `Safe` avg frame time 16.78ms (stdev 0.26 fps) vs. `High` avg frame time 24.33ms (stdev 16.43 fps, 3/10 runs collapsing to 19-47 fps).
- [x] FPS evidence is gathered across repeated bounded runs, not a single run.
- [x] At least 10 bounded runs per profile are recorded. — exactly 10 each, via `--diagnostic perf-sweep`.
- [x] The report includes average FPS, min FPS, max FPS, and variance/range per profile. — see `AUDIT.md` Entry 13 and the `PerformanceProfiles_<timestamp>` package `REPORT.md`.
- [x] No shader-art changes are included in this pass. — `stellar_nursery.frag`'s only diff is the pre-existing parked Detail Pass 2 work, untouched.
- [x] No unbounded Cosmic Engine process is left running at any point. — verified after every command, including the ~2-minute sweep.
- [x] `AUDIT.md` reviewer sign-off remains blank (not self-signed by the implementation model).

## Open risk — FPS variance — CONFIRMED REAL, root cause still unknown

Recent diagnostics across the last three Stellar Nursery visual passes showed mostly stable 57-59 FPS
readings, but also anomalous low readings around 26 FPS and 45 FPS, repeatedly attributed to "system
load" without proof. **P1's 10-run-per-profile sweep confirms this is a real, reproducible phenomenon,
not noise:** at full resolution (`High`), 3 of 10 runs collapsed to 18.94-47.46 avg fps while the other
7 sat at 56.53-59.76 (vsync-capped); at half resolution (`Safe`), all 10 runs stayed within a tight
58.87-59.75 band with zero collapses. This strongly correlates the dips with running at/near this GPU's
real-time budget ceiling rather than pure chance — but the *specific mechanism* (GPU power state,
thermal, OS scheduling, or genuine external contention) is still not isolated, and no system-load
correlation tooling was added in this pass (out of scope — see `AUDIT.md` Entry 13 Known Limitations).

**Update (Entry 20, Mac mini):** on the new Mac mini M4 Pro baseline, StellarNursery no longer shows this collapse at either profile (10-run ranges ≤1.5 fps). LavaLamp High does show a comparable-in-kind dip on this hardware (10-run range 12.4 fps, 63.9-76.4) — the pattern has not disappeared, it has moved to a different world. Not investigated further in Entry 20 (baseline-measurement pass only).

## P2 requirements (OptiPlex + Real Guitar/Scarlett Validation, for reference)

- Physical run on the Dell OptiPlex 5070 Micro (never yet tested). Testing through Entry 19 was on the
  now-retired dev machine's Intel Iris Graphics 6100; from Entry 20 (Mac mini Baseline Validation) onward
  the active dev/art-review baseline is the Mac mini M4 Pro — neither is the OptiPlex stage target.
- FPS evidence captured on that real stage hardware, per profile.
- Actual audio interface identity confirmed and reconciled in docs — project intent is believed to be a
  Scarlett 2i2, but some existing docs/code still reference a Focusrite Clarett. This must be resolved,
  not left ambiguous.
- Real guitar signal confirmed driving FFT values (not just "capture device opened").
- Screenshot/video or log evidence proving audio reactivity with a real signal.

## Build / run status

- `dotnet build`: succeeds, 0 warnings, 0 errors.
- `dotnet run`: starts, logs OpenGL renderer/vendor/version, loads `StellarNursery`, and renders continuously. **StellarNursery now renders a visible (non-black) frame** — see Baseline Recovery Pass 2 below.
- `dotnet run -- --smoke-test`: runs for a fixed 8s window, prints an average-fps/frame-time summary, then exits cleanly (exit code 0, verified no orphaned process).
- `dotnet run -- --diagnostic baseline`: same bounded run, plus writes `DiagnosticReports/Baseline_<timestamp>/REPORT.md`.
- `dotnet run -- --diagnostic visual` (new): cycles solid-color → debug-gradient-shader → normal StellarNursery (2s each), capturing a PPM screenshot + average-luminance/non-black-% reading per phase, writes `DiagnosticReports/Visual_<timestamp>/REPORT.md`, exits cleanly. All three phases confirmed non-black on this machine (StellarNursery: 0.318 avg luminance, 100% non-black pixels).

## Baseline Recovery Pass 2 / Entry 6 — black screen root cause

`Worlds/World01_StellarNursery/Shaders/stellar_nursery.frag` declares 3D camera-basis uniforms (`uCamPos`, `uCamForward`, `uCamRight`, `uCamUp`) that `StellarNursery.cs` never set. Left at their GLSL zero-default, `uCamForward`/`uCamRight`/`uCamUp` were all `vec3(0)`, so the shader's `rayDir = normalize(uCamForward + ...)` evaluated to `normalize(vec3(0))` — a `0/0` NaN — which poisoned the entire raymarch and rendered black. `uBassCombined` (drives the final brightness envelope) was also never set, compounding the dimness. Fixed by setting a fixed camera basis (~200 ly out, looking at the origin, per the shader's own doc comment) and `uBassCombined = max(calibrated bass1, calibrated bass2)` in `StellarNursery.Render()`. No shader logic, art direction, or scene composition was changed.

**Entry 6 confirmed via full git history:** this defect has been present since the very first implementation commit (`951bfee`) — no commit ever set these uniforms. There is no committed "last known good" Stellar Nursery state to roll back to. Entry 6 also found and fixed an unrelated build-hygiene bug: `CosmicEngine.App.csproj` had no exclude for `DiagnosticReports/`, so a previously-generated review package's `source_context/*.cs` snapshot was picked up by the default compile glob and broke `dotnet build` with duplicate-symbol errors. Fixed with a `<Compile Remove="DiagnosticReports/**" />` exclude.

## Original bug status — square/rectangular star artifacts (FIXED, Entry 7)

The original reported bug — stars rendering as square/rectangular shapes instead of points — is fixed. `stellar_nursery.frag`'s star block no longer floors `rayDir` into a voxel and lights the whole cell; a new `pointStarLayer()` helper places a jittered star center inside each qualifying cell and applies a `smoothstep`-based radial falloff bounded to zero outside a small `radius`, so a star is now a small soft dot, never a filled cell. Confirmed visually across multiple random seeds in Entry 7's review package (`after_stellar_full_frame.png`, `after_star_closeup.png`, `after_luminance_debug.png`) — no square/rectangular/triangular patches in any run. Star design (density, radius, color) is intentionally conservative and may need a later artistic-polish pass.

## Visual Recovery Pass 1 / Entry 8 — dark/sparse scene root cause and fix

Under silence, `uBassCombined = 0` collapsed the shader's final brightness envelope (`color *= uDimLevel + uBassCombined * uBassBrightness`) to just `uDimLevel` (was `0.20`) — the single largest brightness suppressor. Separately, `nebulaDensity()`'s threshold (`0.42`) combined with the dominant fbm octave's ~1000 ly scale (larger than the 400 ly march range) meant a fixed camera + a given `uSeed` effectively sampled one large-scale value for the whole frame, often landing below threshold almost everywhere.

Fix: `Tuning.DimLevel` raised to `0.55`; `nebulaDensity()` threshold lowered to `0.38`; raymarch extinction coefficient lowered from `0.80` to `0.35` (an intermediate attempt — threshold 0.30 alone with the original 0.80 coefficient was tried and rejected: it saturated transmittance to zero within 1-2 march steps, producing a uniform "fog wall" instead of a dark wash — same flatness, different color). A diagnostic-only `COSMICENGINE_SEED` env var override was added to `StellarNursery.Load()` for reproducible captures; seed `400` is the current recommended known-reasonable value.

## Known limitation carried forward — seed-dependent brightness (mitigated in Entries 16-17)

Seed-dependent variation is reduced but not eliminated: 5 test seeds (100, 250, 400, 555, 777) with the new tuning ranged 0.09–0.45 avg luminance and 0.005–0.20 per-frame range — some seeds still land in a fairly flat region, though none reproduced the original near-zero-range "flat wash" or "solid fog wall" failure modes. Not fully fixed — would require camera-path or scene design changes, out of scope for a diagnostic-recovery pass.

**Update (Entry 16, Stellar Nursery Showability Audit):** this limitation turned out to be far more severe than "some seeds land in a fairly flat region" suggested — a broader 16-seed sample found roughly 60-70% produced a genuinely flat, structureless gradient (not just dimmer/less-detailed, but the exact "no stars/no structure" failure the user reported). Mitigated (not architecturally fixed) by constraining `StellarNursery.Load()`'s random seed selection to a small pool of empirically-confirmed-good seeds (400, 33, 610, 5).

**Update (Entry 17, Stellar Nursery Showability Revision):** changing `compositionOffset` to fix the corners/empty-center complaint invalidated the Entry 16 pool entirely (a different offset is a different effective sample point per seed) and revealed a further wrinkle: the fragility can also manifest **over time** within a single seed, not just across seeds, since the faster time-evolution from Entry 16 can drift a good-at-launch seed toward a flat state within 15s. Re-curated pool `{777, 33, 61, 155}`, verified at both t=1s and t=15s. Still a curated-pool mitigation, not an architectural fix — see `AUDIT.md` Entry 17.

## Visual Detail Pass 1 / Entry 9 — texture, dust lanes, color, depth, composition (REJECTED, see Entry 10)

`stellar_nursery.frag` gained a 4th fbm octave (0.027 ly⁻¹, ~37 ly scale) for fine wisp/knot texture; its raw sample is reused (via a new `out float fineOctave` param on `fbm3D()`, zero extra cost) as a dust-lane erosion mask in `nebulaDensity()`. A separate cheap `noise3D()` sample drove a `warmPocket` mask added directly to `emitCol`. A free depth cue (reusing the already-computed march distance `t`) tints near material warmer/brighter and far material cooler/dimmer. A constant `compositionOffset` shifts the sampled density-field position (not any camera uniform) for off-center framing. ChatGPT rejected this pass: the warm-pocket mask used a hard `step(0.05, d)` gate that produced a visible hard vertical seam and a flat, posterized "sticker" look rather than volumetric emission — see Entry 10 for the fix.

## Visual Detail Pass 1 Revision / Entry 10 — softening the hard-edged artifacts (ACCEPTED, commit `1150667`)

Root cause (confirmed via `DensityDebug`, which showed the underlying density field is smooth): the hard seam/posterization was a pure color-mapping artifact, not a geometry/density bug. `step(0.05, d)` (a true binary switch) gated the warm glow on/off exactly at a density contour. Fix: replaced with `smoothstep(0.02, 0.10, d)`; broadened and reduced the warm-pocket mask/intensity (`smoothstep(0.55, 0.85, warmNoise)`, additive capped ~(0.77,0.34,0.20), was ~(1.30,0.65,0.47)); broadened and eased the dust-lane erosion (`smoothstep(0.50, 0.90, fineDetail)`, `mix(1.0, 0.70, dustMask)`, was 0.62-0.82 / `mix(1.0, 0.35, ...)`). Fine texture, depth tint, and composition offset unchanged. Revised full-frame metrics are intentionally *lower* in contrast than the rejected version (range 0.206 vs 0.461, stddev 0.025 vs 0.045) since the harsh contrast was itself the artifact — screenshot/visual judgment, not metrics, drove this pass. FPS unaffected (58.2→59.7-59.9 avg on stable runs). Reviewer accepted this as complete but flagged the result as still soft/blurry/low-frequency/under-detailed.

## Visual Detail Pass 2 / Entry 11 — finer texture, tendrils, cool pockets

Follow-up to the accepted, soft Revision baseline. Three additions to `stellar_nursery.frag`, all avoiding hard `step()` gates: (1) a free `sin`/`cos`-only domain warp (14 ly amplitude, per-axis frequency/phase) applied to the density-sampling position before the fbm lookup, bending the field into curved/organic tendril-like silhouettes instead of round blobs; (2) a fine wisp modulation — one extra high-frequency (~11 ly) `noise3D()` sample used as a smooth `mix(0.82, 1.22, smoothstep(...))` multiplier on the already-thresholded density, riding on existing density (0 stays 0) rather than a new threshold; (3) a cool/teal emission-pocket counterpart to the existing warm pocket, same smoothstep-gated mechanism (reuses the existing density gate), distinct frequency/phase, for richer warm/cool interplay. Dust lanes, warm pockets, and composition offset unchanged from the accepted Revision. Before/after at seed 400: full-frame range 0.206→0.344, stddev 0.025→0.038; density/radiance debug both show increased fine structure without harder dust-lane edges (re-verified against the before crop). FPS unaffected on stable runs (58.2-58.6→57.3-59.1 avg).

## Architecture summary

- **Engine loop** (`Engine/CosmicEngine.cs`): owns the OpenTK window, the `Camera`, and the single active `IWorld`. Each frame builds an `AudioSignal` snapshot, updates the camera, renders the active world into a `RenderTarget` sized by the active `PerformanceProfile`'s RenderScale (P1, Entry 13 — was a fixed 1280x720; now `Safe`=640x360, `Balanced`=960x540, `High`=1280x720, default `High`), then blits that to the actual window framebuffer regardless of size (shader cost scales with RenderScale, not window size). Parses `--smoke-test` / `--diagnostic baseline` / `--diagnostic visual` / `--profile <name>` / `--world <name>` (Entry 14) from `args` (passed through from `Program.cs`, which separately dispatches `--diagnostic perf-sweep` to `PerformanceSweep` before any window is constructed). All bounded modes force `Environment.Exit(0)` in `OnUnload` to guarantee the process cannot outlive the window (sweep sub-runs are the one exception, by design — see `PerformanceSweep.cs`).
- **Performance profiles** (`Engine/PerformanceProfile.cs`, `Engine/PerformanceSweep.cs`, new in P1/Entry 13): named RenderScale tiers (`Safe`/`Balanced`/`High`) and a repeated-run sweep orchestrator (`--diagnostic perf-sweep`) that runs N bounded sub-runs per profile in one process and writes raw CSV + summary evidence — see `AUDIT.md` Entry 13 for the first real dataset this produced. `--diagnostic perf-sweep` also accepts `--world <name>` (Entry 14) to target a specific world.
- **Worlds** (`Engine/IWorld.cs`, `Worlds/`, `Engine/WorldSelector.cs`, `Engine/SceneRegistry.cs`): a world implements `Load()/Update()/Render()/Unload()`. Two exist as of Entry 14:
  - `Worlds/World01_StellarNursery/StellarNursery.cs` (default) — a volumetric raymarched nebula shader reacting to two guitar audio channels (Guitar 1 = "Creator": energy/color; Guitar 2 = "Sculptor": density/structure). Correctly sets the shader's 3D camera basis and `uBassCombined` (see above).
  - `Worlds/World02_LavaLamp/LavaLampScene.cs` (Entry 14 draft v0.1, art-upgraded in Entry 25 to v0.2) — a cheap analytic 2D metaball-field fullscreen shader (no raymarch, no noise/hash sampling), proving the multi-world architecture and giving a safe, always-visible, integrated-GPU-friendly scene. Same Guitar 1/Guitar 2 semantic convention (color/glow vs. size/distortion). `LavaLampScene.cs` itself has zero diff since Entry 14 — all v0.2 art changes are confined to `lava_lamp.frag`.
  - `Engine/SceneRegistry.cs` (Entry 15) is now the single source of truth for scene metadata (Id/DisplayName/Description/Status/DefaultProfile/Showable/Factory); `WorldSelector.Create(name, camera)`/`TryParse` (used by `CosmicEngineApp.Run()`/`RunSweepSubRun()` and `--world` CLI selection) are a thin wrapper over it, so CLI parsing and the dashboard's scene cards share one list. Unknown names warn and fall back to `StellarNursery` (never crash), same pattern as `PerformanceProfile.TryParse`.
- **Audio pipeline** (`Audio/`): `AudioEngine` captures stereo audio via OpenAL on a background thread, runs an FFT per buffer (MathNet.Numerics), and exposes `Bass`/`Mid`/`Treble`/`Level` per channel, plus a static `IsCapturing` flag used by the perf logger.
- **Live tuning + Scene Dashboard** (`ControlServer.cs`, `Tuning.cs`, Entry 15): a plain `HttpListener` on `http://localhost:8080` serves one page combining (1) the original audio-tuning sliders bound to `Tuning.*` fields for live soundcheck adjustment, and (2) a Scene Dashboard section — a live status bar (world/profile/fps/render target/audio), scene cards (built from `GET /scenes`, reading `SceneRegistry`) with Launch Safe/Launch High/Restart buttons, and a Quit Engine button. `POST /launch`, `/restart`, `/quit` call thread-safe `CosmicEngineApp.RequestSwitch`/`RequestRestart`/`RequestQuit` methods (`CosmicEngineApp.Current` exposes the live show-mode instance), applied on the render thread in `OnRenderFrame` — enabling **live, in-process scene and profile switching** with no process restart, gated off during any bounded diagnostic run. `run-show.sh`/`Run Cosmic Engine.command` start the engine in unbounded "show mode" and auto-open the dashboard.
- **Diagnostics** (`Diagnostics/Shaders/`): an isolated debug gradient shader (no uniforms, no dependency on world state) used only by `--diagnostic visual` to prove the shader/quad/uniform pipeline independently of any world. `--diagnostic visual` now runs 5 phases (Entry 8): SolidColor, Gradient, StellarNursery, DensityDebug, RadianceDebug — the latter two gated by `StellarNursery.DebugMode` (0 in all normal rendering) and a matching `uDebugMode` shader uniform, reusing the raymarch's already-computed transmittance/radiance to visualize density and raw emission structure in isolation.
- **Render resolution now scales with the active profile (P1); raymarch step count is still hardcoded** — 16 march steps in the shader, unaffected by RenderScale. No shader-quality tiers exist yet (out of scope for P1 — see `ROADMAP.md`).

## Known limitations

- **No shader-quality profile tiers yet.** P1 (Entry 13) implemented RenderScale only; march-step count, octave count, etc. are still fixed constants regardless of profile. Would be a natural P1 follow-up if RenderScale alone proves insufficient on the OptiPlex.
- **Diagnostic tooling is minimal, not a full suite.** Short (~2-8s) sample windows, no multi-scenario sweep, no historical comparison. Screenshots are raw PPM, not PNG (no image-encoding dependency added).
- **The fixed StellarNursery camera is static** — a minimal fix to stop the scene from being black, not a designed camera path. Camera motion/framing is a visual-polish decision, out of scope for this pass.
- **FPS baseline is measurable but not yet recorded as an accepted number.** Observed ~48-60 fps on the current dev machine (Intel Iris Graphics 6100); no target/pass-fail threshold agreed yet.
- **No physical OptiPlex validation yet.** All runtime verification so far has run on the current dev machine only.
- **Real guitar hardware input validation pending.** `Tuning.cs` documents calibration for a Focusrite Clarett interface; more recent references (outside this repo's code) mention a Scarlett 2i2 — whichever is the current physical interface, live two-channel guitar signal has not been re-verified against this build.
- **Tuning/calibration duplication is still unresolved.** `Tuning.cs` holds the live-tunable (control-server-backed) calibration constants; `StellarNursery.cs` keeps its own separate copy of floor/max constants used elsewhere in `Update()`. The two are not kept in sync automatically — documented in `CLAUDE.md`.
- **Dead top-level `Shaders/` folder has been removed** (commit `99f40dc`) — no longer a concern.
- **No live mid-session screenshot capture yet (Entry 15).** The engine's `GL.ReadPixels` capture is only wired into the bounded `--diagnostic visual` path; there is no `POST /screenshot`-style hook for capturing what's actually on screen during a live show-mode/dashboard session. Recommended next action for Entry 15.
- **Scene Dashboard has no thumbnails yet** (`SceneDefinition.ThumbnailPath` is `null` for both scenes) and no experimental scenes exist yet to exercise the "Show experimental scenes" checkbox (implemented, unused).

## Visual Composer Sandbox (World06, 2026-07-17)

A ninth scene, `Worlds/World06_VisualComposer/` (Id `VisualComposer`), was added as a throwaway-
quality artistic-exploration sandbox — explicitly outside the phased video-atoms roadmap (no Scene
Director, no AI selection, no clip database). Nine hardcoded compositions pair one approved
`VisionBoard/` atom with a `StellarNursery` child world via the same video/child/composite seam
World05 HybridTest proved, plus two small inline shader techniques (motes, raking light). Cycled
from the dashboard via `Tuning.ComposerIndex`, no recompile required. See `AUDIT.md` Entry 51 for
the full evidence package and an honest artistic verdict.

## Media Console redesign pass (2026-07-18)

Four independently-committed changes to `MediaConsole/CosmicEngineMediaConsole_20260718_102125/`,
each verified working before the next started (commits `0d1f3ff`, `413a12a`, `ca5cc43`, `a32a8c2`):
the Live Guitar Control panel no longer leaks onto the Visual Exploration tab; the Audio Tuning
panel's mapping list is now Color/Saturation only (defaulting to source "attack", per the user's own
finding that it was the only audio parameter with a noticeable effect) — the Liquid Warp/Edge
Glow/Mirror mappings referenced in the Entry 52 note above no longer exist; the Visual
Exploration/Audio Tuning tabs got a general layout cleanup (collapsible effect groups, dead readouts
removed); and a new toggleable "Queen Cosmic" band-logo fade-in-on-silence feature was added (Cinzel
Decorative wordmark, gold gradient, halo/shimmer/twinkle CSS effects, two fade-duration sliders,
2s dwell). Full detail, evidence, and judgment calls flagged for user review: `AUDIT.md` Entry 53.

**Band-logo scale-to-stage fix + backdrop darkness control (2026-07-19):** fixed the wordmark not
scaling correctly between the small preview card and true fullscreen (was using viewport-relative
`vw` units instead of scaling with the actual stage element - now a CSS container-query fix), and
added a "Backdrop darkness" slider (0-100%, default 60%) so the dimmed video behind the wordmark can
be pushed closer to solid black. See `AUDIT.md` Entry 54.

**Band-logo edge fade + two-line stacked wordmark (2026-07-19):** the backdrop's left/right edges now
fade to black via a mask-image instead of showing a hard rectangle in fullscreen; the wordmark is now
stacked "Queen" / "Cosmic" on two lines instead of one, matching the originally-referenced cover art.
See `AUDIT.md` Entry 55.

**Band-logo wordmark sized up (2026-07-19):** now 8.8% of stage width (was 5.6%), picked by the user
from three Artifact-rendered size options. See `AUDIT.md` Entry 56.

**Audio reactivity simplified to attack-only (2026-07-19):** the configurable Color/Saturation mapping
(sensitivity/response-speed/smoothing/max-contribution/decay-time/dead-zone/source) is removed from
the UI for now. `modulateEffects()` reads guitar attack directly with much larger coefficients
(brightness +90%/saturation +1.8/hue +/-80deg at full attack). Live-confirmed by the user playing real
guitar. Server-side schema left in place, unused, for a future richer-controls pass. See `AUDIT.md`
Entry 57.

**Frozen atom removed + chromatic aberration bug fixed (2026-07-19):** `ce-va2-a2be6a90a2b63f` (fully
frozen, found via full-library `ffmpeg freezedetect` scan) rejected and removed from the runtime
library, now 280 approved atoms. Chromatic Aberration's ghost-copy saturation was hardcoded to 300%
regardless of the slider - fixed to scale with it, so low settings are now actually subtle. Audio
reactivity no longer touches hue at all (Entry 57's hue modulation removed); brightness/saturation
coefficients pulled back. See `AUDIT.md` Entries 58-59.

**Chromatic aberration - real fix (2026-07-19):** Entry 59's fix was incomplete. The actual bug: ghost
layers always rendered at 100% opacity regardless of the slider, because `drawBasic()`'s own alpha
parameter (hardcoded to `1` at the call site) silently overrode the caller's `ctx.globalAlpha`. Fixed by
passing the real alpha into `drawBasic()` directly. Verified with a controlled same-frame A/B test:
1% and 2% are now indistinguishable. See `AUDIT.md` Entry 60.

**Second frozen atom removed (2026-07-19):** `ce-va2-31d431bc872464` (explosion/mushroom-cloud, 99%
frozen), user-recognized from Entry 58's flagged-but-untouched list. Now 279 approved atoms. See
`AUDIT.md` Entry 61.

**Nudity atom removed (2026-07-27):** `ce-va2-ec728da716330b` (surrealist painting revealing two nude
female figures in its final ~1s, found via metadata-then-visual investigation of
`surrealismanddada.mp4` after per-atom descriptions proved unreliable), confirmed with the user via
screenshot before removal. Now 278 approved atoms. A follow-up dense-sampling search of the same source
file for a second suspected instance found none. See `AUDIT.md` Entry 62.
